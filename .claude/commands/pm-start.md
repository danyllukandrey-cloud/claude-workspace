---
description: План на сьогодні — читає DELIVERY-PLAN.md і DECISIONS.md, показує пріоритети дня.
---

Виклич subagent `pm` у режимі **daily** (в запиті до нього явно напиши слово
"daily" і попроси триматись розділу "Режим daily" з його інструкцій).

Отриманий звіт покажи користувачу як є. Ця команда лише показує план — вона
нічого не редагує й не пише в жоден файл.

Якщо у звіті є блок "Кандидати для docs/ISSUES.md" — покажи його окремо
користувачу і запитай "+"/"-". Пиши в docs/ISSUES.md лише після "+", рядок
точно як показано (з наступним вільним ISS-NN), нічого від себе не додавай.
