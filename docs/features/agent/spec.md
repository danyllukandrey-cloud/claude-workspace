---
status: Draft
owner: "Андрій"
reviewers: ["Tech Lead", "Security Lead"]
updated_at: "2026-08-25"
feature_size: "M"
---

# Spec — agent

> **Glossary:** [CONTEXT](../../CONTEXT.md)
> **Reference module / docs / channels used:** `PRODUCT-BOOK.md` Блок 4, `Product_Vision.md` §3-5, `design-review.md` Блок 0/2/5-7, `architecture-map.md`, наявний код `plan/app/src/` (лише scaffold-заглушки, коду агента ще нема).

## 1. Context

Зараз користувач не має жодного способу занести подію в картку інакше, ніж вручну заповнюючи форму — а весь продукт (`life-area-card`, `structure`) спроєктований і описаний з розрахунком на «чат з агентом» як основний канал (`Product_Vision.md §5`, `design-review.md` Блок 5-7). Без специфікації Агента цей канал лишається обіцянкою без акцептанс-критеріїв і блокує подальшу реалізацію обох уже спроєктованих фіч.

Актуальність зараз: `life-area-card` і `structure` пройшли повний SDD-конвеєр (spec → design → tasks), а `implement` ще не починався (DELIVERY-PLAN «Розробка» 5%) — саме час зафіксувати поведінку агента, перш ніж писати код, який на нього спирається.

Прийнятий підхід: Агент v1 — єдиний персонаж, що перетворює пряме надходження (текст або вкладення — фото, файл тощо, D-54) на запис у картку виключно через явне підтвердження, керується спільним системним промптом (базові правила + меню категорій + власне правило користувача) на дефолтній манері базової LLM, без вибору характеру. Ринкова прогалина (`researcher`, 2026-08-25): жоден з чотирьох знайдених продуктів (MyFitnessPal, ChatGPT Memory, Habit Chat, Memex) не поєднує підтвердження-перед-записом + гібридну пам'ять + власні правила поведінки в одному домен-незалежному агенті. Найгостріший ризик (`devils-advocate`, 2026-08-25): механізм примусу власного правила користувача не спроєктований (D-35) — тихе порушення не буде виявлене чи залоговане. Тому специфікація нижче розводить AC «запис» і AC «дотримання правила» окремо (§5), а сам механізм примусу йде у відкриті питання (§8) з власником Андрієм, не мовчазним припущенням.

**Decision override (критик, 2026-08-25):** вибір характеру агента (D-37, статус «діє») свідомо не реалізується в v1 — D-37 не скасовується, лишається чинним рішенням на майбутню версію. Раціо: характер — це шар тону над тим самим базовим рушієм правил (D-27/D-31/D-35/D-43); v1 постачає рушій без шару тону. Підтверджено з Андрієм 2026-08-25 (§8 тримає рядок відстеження).

Джерела: `Product_Vision.md §3-5`, `PRODUCT-BOOK.md` Блок 4, `design-review.md` Блок 0/2/5, рішення D-25, D-26, D-27, D-30, D-31, D-35, D-37, D-41, D-43, D-54, D-70, D-74.

## 2. Goals

- Перетворювати будь-яке пряме надходження користувача (текст або вкладення) на коректний запис у потрібній картці — завжди з підтвердженням перед записом.
- Дотримуватись правил користувача (базове меню категорій + власне сформульоване правило) на кожній відповіді агента.
- Пам'ятати користувача гібридно між сесіями — не втрачати щойно сказане до того, як воно стало рішенням. Інваріант D-30 («мовчазного запису не буває») стосується запису метрики у картку (AC-02/AC-03); запам'ятовування факту для довгострокової пам'яті (US-06/AC-09) — фонова дія без окремого підтвердження, за винятком випадків, коли той самий факт одночасно є записом у картку.

## 3. Non-goals

- Голосове розпізнавання/синтез — окрема технічна частина (D-41); ця специфікація описує лише поведінку агента після того, як текст уже отримано.
- Вибір «характеру» агента (мовчазний консультант/друг/мотиватор, D-37) — відкладено на майбутню версію; v1 працює на дефолтній манері базової LLM.
- Готові інтеграції зовнішніх конекторів — вже Non-goal `life-area-card/spec.md`, не дублюється тут.
- Проактивна ініціатива й нагадування — виключено D-31/D-43, не в цій версії.
- Вкладення в v1 — лише фото (зображення); PDF, аудіо та інші формати — поза v1 (`clarify`, 2026-08-25).

## 4. User stories

### US-01: Запис текстом або вкладенням
**As a** user
**I want** описати подію своїми словами або надіслати вкладення (фото, файл)
**So that** агент оновить потрібну картку без заповнення форми

### US-02: Підтвердження перед записом
**As a** user
**I want** щоб агент завжди перепитав перед записом
**So that** невірно розпізнане повідомлення ніколи мовчки не змінить мої дані

### US-03: Власне імперативне правило
**As a** user
**I want** задати власне правило, якого агент завжди дотримується («не радь, якщо не питаю»)
**So that** агент поводиться так, як хочу саме я, а не за загальним дефолтом

### US-04: Правило з готового меню
**As a** user
**I want** обрати готові категорії правил, а не писати все з нуля
**So that** я можу швидко задати межі, не знаючи, як точно сформулювати імперативне правило

### US-05: Виправлення помилки вводу
**As a** user
**I want** щоб агент пояснив, що не так, коли я ввів суперечливі дані
**So that** я виправлю це одразу, а не виявлю зіпсоване число тижні по тому

### US-06: Пам'ять між сесіями
**As a** user
**I want** щоб агент пам'ятав те, що я вже казав раніше, без повторів
**So that** я не почуваюсь так, ніби щоразу розмовляю з незнайомцем

### US-07: Розпізнавання правильної картки
**As a** user
**I want** щоб агент сам зрозумів, до якої картки належить повідомлення
**So that** мені не треба щоразу явно називати картку

### US-08: Автоматичні звіти активності
**As a** user
**I want** щоб агент сам формував тижневий/місячний/квартальний звіт (D-70)
**So that** я бачу підсумок активності, не збираючи його вручну

### US-09: Онбординг через агента
**As a** user
**I want** отримати коротке вітання й гайд через той самий чат з агентом одразу після встановлення (D-74)
**So that** я розумію, у що вписуюсь, перш ніж щось вводити

## 5. Acceptance criteria

### AC-01 (US-01) — happy path
**Given** користувач має активну картку й описує подію вільним текстом
**When** повідомлення надіслано
**Then** агент визначає ймовірну картку й метрику та пропонує конкретний запис, перш ніж щось зберегти

### AC-10 (US-01) — happy path
**Given** користувач надсилає вкладення (наприклад фото сторінки книги) без жодного тексту
**When** агент розпізнає вкладення як самодостатній факт
**Then** агент пропонує запис на основі вкладення так само, як для тексту — саме вкладення замінює текстовий опис повністю

### AC-02 (US-02) — happy path
**Given** агент запропонував конкретний запис і чекає на відповідь
**When** користувач підтверджує
**Then** система записує подію в картку і показує користувачу, що це збережено

### AC-03 (US-02) — domain invariant
**Given** агент запропонував запис
**When** користувач явно не підтвердив (мовчить або пише щось не по темі)
**Then** система нічого не записує — мовчазного запису не буває

### AC-10b (US-01) — error
**Given** користувач надсилає вкладення нерозпізнаного/нечитабельного/непідтримуваного типу (не фото, або фото, з якого агент не може виділити факт)
**When** агент це виявляє
**Then** агент пояснює, чому не може обробити вкладення, і просить текстовий опис замість нього — симетрично AC-04

### AC-02b (US-02) — happy path
**Given** агент запропонував конкретний запис і чекає на відповідь
**When** користувач замість «так» надсилає уточнення до деталі пропозиції (наприклад «ні, 3 км», а не 5)
**Then** агент оновлює пропозицію відповідно до уточнення й повторно чекає підтвердження — без запуску нового діалогу з нуля

### AC-04 (US-05) — error
**Given** користувач вводить дані, які суперечать уже відомому, або повідомлення, яке не мапиться на жодну відому метрику
**When** агент це помічає
**Then** агент пояснює простими словами, що саме незрозуміло чи суперечливо, і просить уточнити — не вгадує і не мовчить

### AC-05 (US-07) — cross-context
**Given** повідомлення користувача могло б стосуватись кількох наявних карток або жодної
**When** агент намагається визначити, куди його записати
**Then** агент питає користувача, яку картку мав на увазі, або пропонує створити нову — не вгадує сам

### AC-06 (US-01) — authorization
**Given** користувач автентифікований через Google-вхід
**When** агент читає чи записує будь-які дані картки
**Then** система обмежує кожну дію лише картками й Структурою саме цього користувача — навіть якщо в тексті згадано чиєсь інше ім'я чи дані

### AC-07 (US-03) — happy path
**Given** користувач задав власне імперативне правило («не радь, якщо не питаю»)
**When** виникає ситуація, де базова модель за замовчуванням дала б непрохану пораду
**Then** відповідь агента слідує правилу користувача, а не дефолтній поведінці

### AC-08 (US-04) — happy path
**Given** користувач відкрив налаштування правил
**When** обирає одну чи кілька категорій з готового меню, без вільного тексту
**Then** система зберігає обрані категорії як активні правила, і подальші відповіді агента їх відображають

### AC-09 (US-06) — happy path
**Given** користувач у попередній сесії повідомив значущий факт чи рішення
**When** пізніша сесія торкається тієї самої теми
**Then** відповідь агента враховує цей факт без повторного пояснення від користувача

### AC-11 (US-08) — happy path
**Given** настав час чергового автоматичного звіту (тижневого, місячного чи квартального)
**When** агент формує його за визначеними правилами
**Then** агент формує звіт активності (`activity-report`, `CONTEXT.md`) — окрема від Літопису Структури сутність — і подає його як пасивний запис: агент нічого не надсилає й нікого не перериває

### AC-12 (US-04) — happy path
**Given** користувач перебуває на конкретній картці й хоче, щоб правило діяло тут інакше, ніж глобально
**When** задає перевизначення саме для цієї картки
**Then** система застосовує перевизначене правило лише на цій картці, а глобальне лишається чинним для решти — перевизначення картки свідомо переважає глобальне правило (AC-07/AC-08) на цій картці; це очікуваний намір користувача, а не суперечність, яку потрібно розв'язувати

### AC-13 (US-09) — happy path
**Given** користувач щойно встановив застосунок
**When** відкриває його вперше
**Then** агент через чат подає коротке вітання і короткий гайд, без окремої статичної сторінки — це одноразовий, свідомо дозволений виняток із правила «агент не заговорює першим» (D-43, `CONTEXT.md` Invariants): онбординг відбувається рівно один раз після встановлення й не є проактивним нагадуванням чи ініціативою під час звичайного використання

### AC-14 (US-03) — happy path
**Given** користувач хоче сформулювати власне правило, але не знає точного формулювання
**When** звертається до агента з чорновою ідеєю правила
**Then** агент допомагає сформулювати правило в діалозі й перевіряє його на несуперечливість із вже наявними правилами тієї самої області дії (глобальне звіряється лише з глобальними, перевизначення картки — лише з іншими правилами тієї самої картки), перш ніж зберегти; перевизначення картки (AC-12) свідомо суперечить глобальному правилу за задумом — це не предмет цієї перевірки

### AC-15 (US-06) — happy path
**Given** користувач щось сказав у поточній сесії, ще не оформлене як довгострокове рішення
**When** та сама сесія триває далі
**Then** агент не «забуває» щойно сказане до кінця сесії, навіть якщо воно ще не потрапило в довгострокову пам'ять

## 6. Non-functional requirements

| Aspect | Target | Measurement |
|---|---|---|
| Latency p95 «агент розпізнав і запропонував запис» (AC-01) | ≤ 4000 ms | час від надсилання повідомлення до появи пропозиції в чаті |
| Latency p95 підтвердженого запису (AC-02) | ≤ 1000 ms | час від підтвердження до оновлення лічильника на картці |
| Throughput | ≥ 5 req/s на інстанс | навантажувальний smoke-тест перед запуском |
| Availability | 99.0% | місячне вікно — один бекенд-проксі (D-24), без резервування зараз |
| Точність дотримання власного правила (AC-07) | TBD | залежить від невирішеного механізму примусу (D-35) → §8 |

## 6.1 Security / privacy

- **Data classification:** confidential — особисті дані про життя користувача (картки, факти, рішення).
- **Personal data touched:** так — текст повідомлень, довгострокові факти в пам'яті агента (можуть включати ім'я, здоров'я, рішення) — нова чутлива поверхня даних.
- **AuthZ/AuthN impact:** кожен запит прив'язаний до автентифікованої сесії (Google-вхід, D-33); усі читання/записи скеровані лише на Структуру й картки цього користувача (AC-06).
- **Abuse cases:**
  - крос-користувацький доступ до чужих карток → блокується на рівні бекенда (AC-06)
  - отруєння/невидалюваність довгострокової пам'яті (правдоподібна хибна теза потрапляє в пам'ять і незвідки видалити) → механізм редагування/видалення не спроєктований — §8
  - спам-записи → ліміт частоти на бекенді, конкретне число не вирішено — §8
  - injection через текст повідомлення/вкладення, що намагається змусити агента порушити власні чи базові правила → агент лишається в межах системного промпту ПЛАНу незалежно від вмісту вхідного тексту
- **Security review:** Required — нова фіча M-розміру з новою чутливою поверхнею даних (довгострокова пам'ять).

## 7. Metrics / KPIs

- **Частка записів через чат, підтверджених без виправлення** — baseline: 0 (нова фіча), target: ≥80% протягом перших 30 днів використання.
- **Медіанний час від повідомлення до підтвердженого запису** (весь цикл AC-01→AC-02) — baseline: невідомо, target: ≤10 секунд медіанно, вимірюється протягом 90 днів після запуску.
- **Частка повідомлень, де агент правильно визначив картку без уточнюючого питання** (AC-05) — baseline: невідомо, target: ≥90%, вимірюється протягом 90 днів після запуску.

## 8. Open questions

- [ ] Вибір характеру агента (D-37: мовчазний консультант/друг/мотиватор) — реалізувати в майбутній версії, не v1. Default now: v1 працює на дефолтній манері базової LLM без вибору характеру (критик 2026-08-25, decision override у §1). — owner: Андрій, due: наступна версія після v1
- [ ] Механізм гарантованого виконання власного імперативного правила користувача (AC-07, D-35) — як саме забезпечити, щоб правило не порушувалось тихо? Default now: AC описує лише спостережену поведінку, примус не гарантований технічно. — owner: Андрій, due: перед `/sdd:design agent` (потрібне зовнішнє дослідження курсу «агентів воркфлоу», Крок 7 опитувальника овнера)
- [ ] Редагування й видалення довгострокової пам'яті користувачем («забудь це») — механізму немає (`devils-advocate`, вектор отруєння/невидалюваності пам'яті). Уточнено (`clarify`, 2026-08-25): запам'ятовування факту в пам'ять відбувається без окремого підтвердження (§2) — саме тому цей механізм редагування/видалення критичний, не опціональний. Default now: не реалізовано в v1. — owner: Андрій, due: перед `/sdd:design agent`
- [ ] Число сесій у короткостроковому сирому вікні гібридної пам'яті (D-26) — деталь реалізації. Разом із цим невизначена сама одиниця «сесія» (US-06/AC-09/AC-15): одне відкриття чату, вікно неактивності, чи календарний день (`clarify`, 2026-08-25). Default now: не фіксується зараз. — owner: Андрій, due: `/sdd:design agent`
- [ ] Час життя пропозиції, що чекає підтвердження (AC-03): чи «згорає» через певний час, чи діє одна активна пропозиція на користувача, чи може бути черга кількох. `clarify`, 2026-08-25. Default now: не визначено. — owner: Андрій, due: перед `/sdd:design agent`
- [ ] Склад куратованого меню категорій правил (AC-08, D-27) — перелік самих категорій, не механізм примусу (той — окремий рядок вище, D-35). `clarify`, 2026-08-25. Default now: не визначено. — owner: Андрій, due: перед `/sdd:design agent`
- [ ] Що агент робить зі згадкою третьої особи в тексті (AC-06) — записує весь опис разом із чужим ім'ям у пам'ять користувача, чи прибирає згадку й лишає тільки вимірюване число. Торкається приватності третіх осіб (`clarify`, 2026-08-25, →§6.1). Default now: не визначено. — owner: Андрій, due: перед `/sdd:design agent`
- [ ] Ліміт частоти повідомлень (rate limit) проти спаму й економіки важкого вводу (`devils-advocate`, вектор економіки мультимодального вводу) — конкретне число не вирішено. Default now: TBD. — owner: Андрій, due: перед `/sdd:design agent`
- [ ] Поріг впевненості для «пропоную конкретний запис» (AC-01) vs «питаю, яку картку мав на увазі» (AC-05) — за яким сигналом агент вирішує; впливає на досяжність KPI-3 (≥90% без уточнюючого питання). `clarify`, 2026-08-25. Default now: не визначено. — owner: Андрій, due: перед `/sdd:design agent`
- [ ] NFR-деталі §6: окрема latency-межа для шляху вкладення (AC-10, наразі покриває лише текст) і параметри throughput-тесту (реальний чи заглушений виклик LLM, тривалість, кількість одночасних користувачів). `clarify`, 2026-08-25. Default now: не визначено. — owner: Андрій, due: перед `/sdd:design agent`
- [ ] Поведінка при недоступності LLM-провайдера (§6 Availability 99.0%, D-24) — що бачить користувач: помилку без втрати тексту, чи чергу; чи входить доступність провайдера у вікно 99.0%. Торкається AC-03 (мовчазного запису не буває). `clarify`, 2026-08-25. Default now: не визначено. — owner: Андрій, due: перед `/sdd:design agent`
