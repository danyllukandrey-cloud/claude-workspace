# Зміни — life-area-card (CH-NN)

Процес і статуси — [CLAUDE.md §Правки в наявній фічі (CH-NN, D-133)](../../../CLAUDE.md).

| # | Дата | Юзер-сторі | Юзер-кейс | Статус | Де виконано |
|---|---|---|---|---|---|
| CH-01 | 2026-09-19 | Як користувач ПЛАН, я хочу знайти архів карток прямо на сторінці «Картки», поруч із кнопкою створення нової картки, щоб не йти на іншу сторінку заради дії, повʼязаної саме з картками. | 1. Переходжу на «Картки» — біля кнопки «Створити картку» з'являється кнопка «Архів карток» (по центру). 2. Клік веде в те саме місце, що й однойменна кнопка на сторінці «Схема» (координовано з `structure` CH-01). | виконано | `plan/app/src/cards/life-area-card/ui/DeckScreen.tsx` (кнопка "Архів карток" біля "Створити картку", в обох станах — порожньому й завантаженому), `plan/app/src/app/App.tsx` (спільний callback `openArchive`, app-composition, той самий, що й `structure` CH-01) |
| CH-02 | 2026-09-19 | Як користувач ПЛАН, я хочу мати можливість позначити картку одним із трьох станів (використовується / критично потребує відновлення / на паузі) кольоровим індикатором замість вимірюваних метрик, щоб відстежувати зони життя, які не піддаються кількісному виміру, тим самим кольором-сигналом, що й решта продукту. | 1. Створюю чи редагую картку — на самому початку налаштування бачу новий вибір «Картка: стан без вимірювань», розташований НАД вибором «Картка: постійний процес з метриками (без дати)». 2. Обираю «стан без вимірювань» — усі інші налаштування картки (метрики, цілі, частота) стають неактивними: цей вид картки не вимірюється ніяк, колір і є вимірюванням. 3. Обираю один із трьох станів: використовується (зелений) / критично потребує відновлення (червоний) / на паузі (жовтий). 4. Колір показується м'ячиком у правому верхньому кутку картки — той самий м'ячик, що й позначка зміни запису (`EntryHistoryList.tsx:28`, патерн `chip-gloss`/`STATUS_DOT`, D-120/D-126) — видно на картці, в колоді і (координовано з `structure` CH-02) на Схемі/Аналітиці. Текст форми блоку-метрики заразом уточнюється: «Що рухаємо:» → «Що рухаємо/вимірюємо:», «Постійний процес (без дати)» → «Постійний процес з метриками (без дати)». | виконано | `domain/card.ts` (`CardTrackingMode`/`CardHealthState`, `setTrackingModeState`/`setTrackingModeMetrics`), `infra/postgres-repo.ts` (`tracking_mode`/`health_state`), `app/update-card.ts` (валідація), `ports/card-handlers.ts`, `ui/types.ts`, `ui/CardFace.tsx` (м'ячик), `ui/CardBack.tsx` (вибір режиму, справжній `disabled` на метричних налаштуваннях), `ui/MetricBlockForm.tsx` (текст полів), `src/app/main.tsx` (`onUpdateTracking`, `fetchCardSummaries`), `src/app/App.tsx`; координовано з `structure/ui/LayoutBoard.tsx`/`AnalyticsScreen.tsx` (власний канал, CH-02 там); міграція `docs/features/life-area-card/migrations/09_add_card_tracking_mode.{up,down}.sql` (промоучена `plan/app/migrations/1789841424996_add-card-tracking-mode.sql`); `contracts/openapi.yaml` (`Card`/`CardUpdate` схеми) |
| CH-03 | 2026-09-19 | Як користувач ПЛАН, я хочу мати можливість виправити вже створений блок-метрику (перейменувати, змінити налаштування, перенести на іншу картку), а не лише видалити його, щоб не втрачати історію записів через одруківку чи зміну задуму. | 1. На блоці-метриці, поруч із хрестиком видалення, зʼявляється олівець (правий нижній кут блоку). 2. Клік по олівцю відкриває редагування: можна змінити налаштування (ціль/одиниця/частота), перейменувати метрику, перенести на іншу картку. 3. Перенесення на іншу картку використовує наявну функцію `transferMetricBlock` (уже працює: перевіряє власника, відхиляє архівовані картки, ловить колізію назва+одиниця) — нового бекенду для цієї дії не треба. 4. Перейменування/зміна налаштувань без перенесення — новий функціонал, бекенд-логіки для цього поки нема. | виконано | `app/update-metric-block.ts` (новий use-case: перейменування/налаштування, non-disclosure, колізія назва+одиниця, той самий підхід, що `archive-metric-block.ts`), `ports/metric-block-handlers.ts` (`updateMetricBlock`, `MetricBlockUpdateBody`), `server/app.ts` (`PATCH /cards/{cardId}/metric-blocks/{metricBlockId}`), `ui/MetricBlockCard.tsx` (олівець, справжній `disabled`), `ui/CardBack.tsx` (панель редагування — `MetricBlockForm` перевикористаний + пікер перенесення через наявну `transferMetricBlock`), `ui/types.ts` (`MetricBlockSettings`, `MetricBlockTransferTargetCard`), `ui/DeckFrontCard.tsx`/`DeckScreen.tsx` (`transferTargetCards`), `src/app/App.tsx`/`main.tsx`; `contracts/openapi.yaml` (`MetricBlockUpdate` схема + `PATCH` шлях) |
| CH-04 | 2026-09-19 | Як користувач ПЛАН, я хочу, щоб створення нової картки відбувалось прямо в колоді (порожня картка, яку я заповнюю на місці), а не переходом на окремий повноекранний екран, щоб процес відчувався як частина колоди, а не переривання. | 1. Натискаю «+ Створити картку» в колоді. 2. Замість переходу на новий екран (зараз — `App.tsx` screen-swap на `CreateCardForm`) зʼявляється порожня картка на місці передньої картки колоди. 3. Заповнюю картку на місці (`DeckGrid`'s `renderFront` render-prop — новий шлях для синтетичного елемента «+», замість фіксованого `DeckFrontCard`). 4. Зберігаю — картка стає звичайною карткою колоди. | виконано | `ui/DeckScreen.tsx` (`isCreating`, синтетичний `CREATE_PLACEHOLDER_ITEM`, `handleCreate`), `ui/CreateCardForm.tsx` (обгортка адаптована з `min-h-screen` до слоту картки `h-full`), `src/app/App.tsx` (прибрано `Screen: 'create'`, `onCreateCard` тепер реальний `createCard` напряму) |

| CH-05 | 2026-09-21 | Як користувач, я хочу пункт меню «Редагувати» замість «Перейменувати» на картці, що веде на сторінку редагування лицевої частини — щоб назва дії відповідала тому, що вона реально відкриває. | 1. Три крапки на картці → бачу «Редагувати» (не «Перейменувати») і «Архівувати». 2. Клік на «Редагувати» веде на сторінку редагування лицевої частини картки (Назва + Опис). | виконано | `ui/CardFace.tsx` (меню, `startEdit`) |
| CH-06 | 2026-09-21 | Як користувач, я хочу просту сторінку редагування лицевої частини картки без зайвих проміжних станів, щоб не гадати, чому нижнє поле не реагує на клік. | 1. Відкриваю редагування (CH-05) — поля Назва і Опис одразу редаговані, без проміжного стану «спершу клацни на текст, щоб він відкрився». 2. Чекбокс «Позначити заповненою» прибрано — картка вважається заповненою, якщо збережено непорожній текст (не окрема позначка). 3. Замість двох кнопок «Скасувати» — одна «На зад» (не зберігає) і «Зберегти» (зберігає, якщо є текст). | виконано | `ui/CardFace.tsx` (`isEditing` об'єднує Назву+Опис, `saveEdit` ланцюжком onRename→onUpdateDescription, `markFilled` похідне від тексту, `onRename` викликається лише якщо назва реально змінилась). 3 review-fix-раунди: (1) успішний rename застосовується одразу, незалежно від подальшого відхилення onUpdateDescription; (2) `DeckScreen.tsx`'s `handleRename` патчить кешовану назву в `state.items` НА МІСЦІ, не `reload()` — перша спроба з `reload()` розмонтовувала відкриту картку (втрата звороту/панелі редагування); (3) `app/update-card.ts` — `insertLifecycleEvent('filled')` тепер захищений тим самим `!current.description` guard, що вже мав `recordAction` — інакше CH-06 (markFilled на кожному збереженні) писав повторний "filled" в append-only лог при кожному перейменуванні вже заповненої картки |
| CH-07 | 2026-09-21 | Як користувач, я хочу той самий патерн редагування (перегляд/редагування через ⋯) на звороті картки, що й на лицевій, щоб не плутати редагування самої картки з редагуванням окремої метрики. | 1. Зворот картки за замовчуванням — режим перегляду: кнопка «Додати блок-метрику» зверху, готові блоки метрик, «Історія записів» знизу. 2. Три крапки у правому верхньому кутку звороту (як на лицевій) → «Редагування» і «Архівувати». 3. «Редагування» відкриває налаштування самої картки (не метрик — ті редагуються через олівчик на конкретній метриці, CH-03). | виконано | `ui/CardBack.tsx` (меню, `isEditingBack`-гейт навколо «Режим картки», `ArchiveCardDialog` перевикористаний), `ui/DeckFrontCard.tsx`/`ui/DeckScreen.tsx` (новий обов'язковий проп `cardName`, прокинуто в `onArchive`/`onArchived`). Review-fix: пункт «Редагування» рендериться лише коли є `onUpdateTracking` (без нього нема що показати — клік був би без ефекту) |
| CH-08 | 2026-09-21 | Як користувач, я хочу коректну форму блоку-метрики: помилка зникає одразу, як почав вводити текст, а заголовок форми відповідає дії (створення чи редагування). | 1. Починаю вводити текст у полі «Що рахуємо/вимірюємо» — помилка «Вкажіть, що рахуємо» гасне одразу (не чекає збереження). 2. Відкриваю редагування наявної метрики через олівчик — заголовок форми «Редагування» (не «Новий блок-метрика»). | виконано | `ui/MetricBlockForm.tsx` (`onChange` гасить `labelError`/`unitError`, заголовок за `initialValues !== undefined`) |
| CH-09 | 2026-09-21 | Як користувач, я хочу бачити всі картки архіву одразу, а не колодою, щоб порівнювати їх, не гортаючи по одній. | 1. Відкриваю «Архів карток». 2. Картки розкладені сіткою знизу вгору — той самий принцип, що базове розташування нових карток у «Схемі», не стос/колода. | виконано | `ui/ArchiveScreen.tsx` (CSS grid замість `DeckGrid`; `DeckGrid`/`DeckGridItem` лишаються лише для активної колоди, `DeckScreen.tsx`) |
| CH-10 | 2026-09-21 | Як користувач, я хочу 3 окремі режими картки замість 2, щоб напис «Постійний процес з метриками (без дати)» не позначав одночасно й режим картки, і чекбокс у формі блоку-метрики — та сама назва в двох місцях плутала. | 1. У «Режим картки» тепер 3 варіанти: «стан без вимірювань» (без змін) → «постійний процес» (форма нового/наявного блоку показує лише «Що рахуємо»+«Одиниця», без чекбокса/цілі/дати — картка вже сама каже, що процес постійний) → «з цілями та метриками» (форма лишається повною, як і був старий «metrics»). 2. Колишній `tracking_mode='metrics'` розведено на `'ongoing'`/`'goals'` — потребувало міграції бази (CHECK-обмеження дозволяло рівно 2 значення), існуючі картки перенесено в `'goals'` (найбільш сумісний вибір, нічого не втрачено). | виконано | Міграція `docs/features/life-area-card/migrations/10_expand_card_tracking_mode.{up,down}.sql` (промоучена `plan/app/migrations/1789981137560_expand-card-tracking-mode.sql`, застосована); `domain/card.ts` (`CardTrackingMode` 3 значення, `setTrackingModeOngoing`/`setTrackingModeGoals`), `infra/postgres-repo.ts`, `app/update-card.ts`, `src/app/main.tsx` (DTO), `ui/CardBack.tsx` (3-way radio), `ui/MetricBlockForm.tsx` (новий проп `mode`, ховає чекбокс/ціль/дату для `'ongoing'`), `contracts/openapi.yaml`. Дрібне: зона скролу звороту картки більше не наїжджає на кнопку «...» (`pt-8`) |

## Пов'язане

- Скоординована зміна з [`structure` CH-01](../structure/changes.md) — та сама гілка, кнопка «Архів» переїжджає з Аналітики на Схему й дублюється тут.
- CH-02 скоординована з [`structure` CH-02](../structure/changes.md) — м'ячик стану на Схемі/Аналітиці.
- CH-05…CH-09 — знахідки живого тестування 2026-09-21 (гілка `life-plan-levels`), виконані на окремій гілці `life-area-card-live-fixes` (D-134) після мерджу `life-plan-levels` (PR #13) у `main`.
