# Зміни — life-area-card (CH-NN)

Процес і статуси — [CLAUDE.md §Правки в наявній фічі (CH-NN, D-133)](../../../CLAUDE.md).

| # | Дата | Юзер-сторі | Юзер-кейс | Статус | Де виконано |
|---|---|---|---|---|---|
| CH-01 | 2026-09-19 | Як користувач ПЛАН, я хочу знайти архів карток прямо на сторінці «Картки», поруч із кнопкою створення нової картки, щоб не йти на іншу сторінку заради дії, повʼязаної саме з картками. | 1. Переходжу на «Картки» — біля кнопки «Створити картку» з'являється кнопка «Архів карток» (по центру). 2. Клік веде в те саме місце, що й однойменна кнопка на сторінці «Схема» (координовано з `structure` CH-01). | виконано | `plan/app/src/cards/life-area-card/ui/DeckScreen.tsx` (кнопка "Архів карток" біля "Створити картку", в обох станах — порожньому й завантаженому), `plan/app/src/app/App.tsx` (спільний callback `openArchive`, app-composition, той самий, що й `structure` CH-01) |
| CH-02 | 2026-09-19 | Як користувач ПЛАН, я хочу мати можливість позначити картку одним із трьох станів (використовується / критично потребує відновлення / на паузі) кольоровим індикатором замість вимірюваних метрик, щоб відстежувати зони життя, які не піддаються кількісному виміру, тим самим кольором-сигналом, що й решта продукту. | 1. Створюю чи редагую картку — на самому початку налаштування бачу новий вибір «Картка: стан без вимірювань», розташований НАД вибором «Картка: постійний процес з метриками (без дати)». 2. Обираю «стан без вимірювань» — усі інші налаштування картки (метрики, цілі, частота) стають неактивними: цей вид картки не вимірюється ніяк, колір і є вимірюванням. 3. Обираю один із трьох станів: використовується (зелений) / критично потребує відновлення (червоний) / на паузі (жовтий). 4. Колір показується м'ячиком у правому верхньому кутку картки — той самий м'ячик, що й позначка зміни запису (`EntryHistoryList.tsx:28`, патерн `chip-gloss`/`STATUS_DOT`, D-120/D-126) — видно на картці, в колоді і (координовано з `structure` CH-02) на Схемі/Аналітиці. Текст форми блоку-метрики заразом уточнюється: «Що рухаємо:» → «Що рухаємо/вимірюємо:», «Постійний процес (без дати)» → «Постійний процес з метриками (без дати)». | виконано | `domain/card.ts` (`CardTrackingMode`/`CardHealthState`, `setTrackingModeState`/`setTrackingModeMetrics`), `infra/postgres-repo.ts` (`tracking_mode`/`health_state`), `app/update-card.ts` (валідація), `ports/card-handlers.ts`, `ui/types.ts`, `ui/CardFace.tsx` (м'ячик), `ui/CardBack.tsx` (вибір режиму, справжній `disabled` на метричних налаштуваннях), `ui/MetricBlockForm.tsx` (текст полів), `src/app/main.tsx` (`onUpdateTracking`, `fetchCardSummaries`), `src/app/App.tsx`; координовано з `structure/ui/LayoutBoard.tsx`/`AnalyticsScreen.tsx` (власний канал, CH-02 там); міграція `docs/features/life-area-card/migrations/09_add_card_tracking_mode.{up,down}.sql` (промоучена `plan/app/migrations/1789841424996_add-card-tracking-mode.sql`); `contracts/openapi.yaml` (`Card`/`CardUpdate` схеми) |
| CH-03 | 2026-09-19 | Як користувач ПЛАН, я хочу мати можливість виправити вже створений блок-метрику (перейменувати, змінити налаштування, перенести на іншу картку), а не лише видалити його, щоб не втрачати історію записів через одруківку чи зміну задуму. | 1. На блоці-метриці, поруч із хрестиком видалення, зʼявляється олівець (правий нижній кут блоку). 2. Клік по олівцю відкриває редагування: можна змінити налаштування (ціль/одиниця/частота), перейменувати метрику, перенести на іншу картку. 3. Перенесення на іншу картку використовує наявну функцію `transferMetricBlock` (уже працює: перевіряє власника, відхиляє архівовані картки, ловить колізію назва+одиниця) — нового бекенду для цієї дії не треба. 4. Перейменування/зміна налаштувань без перенесення — новий функціонал, бекенд-логіки для цього поки нема. | виконано | `app/update-metric-block.ts` (новий use-case: перейменування/налаштування, non-disclosure, колізія назва+одиниця, той самий підхід, що `archive-metric-block.ts`), `ports/metric-block-handlers.ts` (`updateMetricBlock`, `MetricBlockUpdateBody`), `server/app.ts` (`PATCH /cards/{cardId}/metric-blocks/{metricBlockId}`), `ui/MetricBlockCard.tsx` (олівець, справжній `disabled`), `ui/CardBack.tsx` (панель редагування — `MetricBlockForm` перевикористаний + пікер перенесення через наявну `transferMetricBlock`), `ui/types.ts` (`MetricBlockSettings`, `MetricBlockTransferTargetCard`), `ui/DeckFrontCard.tsx`/`DeckScreen.tsx` (`transferTargetCards`), `src/app/App.tsx`/`main.tsx`; `contracts/openapi.yaml` (`MetricBlockUpdate` схема + `PATCH` шлях). **Живе тестування 2026-09-21, review-fix:** повторний клік на олівець ТОГО САМОГО блоку, поки його панель редагування вже відкрита, тепер згортає її -- той самий перемикач (toggle), що кнопка "Закрити редагування", а не просто переоткриває той самий стан. Прибрано дубль заголовка "Редагування" (панель уже мала власний "Редагування «label»", форма всередині ще раз показувала голе "Редагування" -- новий проп `MetricBlockForm.showHeading` вимикає внутрішній заголовок лише для цього виклику, форма створення нового блоку його зберігає). "Перенести на іншу картку" -> "Перенести МЕТРИКУ на іншу картку" (без контексту незрозуміло, що саме переноситься) |
| CH-04 | 2026-09-19 | Як користувач ПЛАН, я хочу, щоб створення нової картки відбувалось прямо в колоді (порожня картка, яку я заповнюю на місці), а не переходом на окремий повноекранний екран, щоб процес відчувався як частина колоди, а не переривання. | 1. Натискаю «+ Створити картку» в колоді. 2. Замість переходу на новий екран (зараз — `App.tsx` screen-swap на `CreateCardForm`) зʼявляється порожня картка на місці передньої картки колоди. 3. Заповнюю картку на місці (`DeckGrid`'s `renderFront` render-prop — новий шлях для синтетичного елемента «+», замість фіксованого `DeckFrontCard`). 4. Зберігаю — картка стає звичайною карткою колоди. | виконано | `ui/DeckScreen.tsx` (`isCreating`, синтетичний `CREATE_PLACEHOLDER_ITEM`, `handleCreate`), `ui/CreateCardForm.tsx` (обгортка адаптована з `min-h-screen` до слоту картки `h-full`), `src/app/App.tsx` (прибрано `Screen: 'create'`, `onCreateCard` тепер реальний `createCard` напряму) |

| CH-05 | 2026-09-21 | Як користувач, я хочу пункт меню «Редагувати» замість «Перейменувати» на картці, що веде на сторінку редагування лицевої частини — щоб назва дії відповідала тому, що вона реально відкриває. | 1. Три крапки на картці → бачу «Редагувати» (не «Перейменувати») і «Архівувати». 2. Клік на «Редагувати» веде на сторінку редагування лицевої частини картки (Назва + Опис). | виконано | `ui/CardFace.tsx` (меню, `startEdit`) |
| CH-06 | 2026-09-21 | Як користувач, я хочу просту сторінку редагування лицевої частини картки без зайвих проміжних станів, щоб не гадати, чому нижнє поле не реагує на клік. | 1. Відкриваю редагування (CH-05) — поля Назва і Опис одразу редаговані, без проміжного стану «спершу клацни на текст, щоб він відкрився». 2. Чекбокс «Позначити заповненою» прибрано — картка вважається заповненою, якщо збережено непорожній текст (не окрема позначка). 3. Замість двох кнопок «Скасувати» — одна «На зад» (не зберігає) і «Зберегти» (зберігає, якщо є текст). | виконано | `ui/CardFace.tsx` (`isEditing` об'єднує Назву+Опис, `saveEdit` ланцюжком onRename→onUpdateDescription, `markFilled` похідне від тексту, `onRename` викликається лише якщо назва реально змінилась). 3 review-fix-раунди: (1) успішний rename застосовується одразу, незалежно від подальшого відхилення onUpdateDescription; (2) `DeckScreen.tsx`'s `handleRename` патчить кешовану назву в `state.items` НА МІСЦІ, не `reload()` — перша спроба з `reload()` розмонтовувала відкриту картку (втрата звороту/панелі редагування); (3) `app/update-card.ts` — `insertLifecycleEvent('filled')` тепер захищений тим самим `!current.description` guard, що вже мав `recordAction` — інакше CH-06 (markFilled на кожному збереженні) писав повторний "filled" в append-only лог при кожному перейменуванні вже заповненої картки |
| CH-07 | 2026-09-21 | Як користувач, я хочу той самий патерн редагування (перегляд/редагування через ⋯) на звороті картки, що й на лицевій, щоб не плутати редагування самої картки з редагуванням окремої метрики. | 1. Зворот картки за замовчуванням — режим перегляду: кнопка «Додати блок-метрику» зверху, готові блоки метрик, «Історія записів» знизу. 2. Три крапки у правому верхньому кутку звороту (як на лицевій) → «Редагування» і «Архівувати». 3. «Редагування» відкриває налаштування самої картки (не метрик — ті редагуються через олівчик на конкретній метриці, CH-03). | виконано | `ui/CardBack.tsx` (меню, `isEditingBack`-гейт навколо «Режим картки», `ArchiveCardDialog` перевикористаний), `ui/DeckFrontCard.tsx`/`ui/DeckScreen.tsx` (новий обов'язковий проп `cardName`, прокинуто в `onArchive`/`onArchived`). Review-fix: пункт «Редагування» рендериться лише коли є `onUpdateTracking` (без нього нема що показати — клік був би без ефекту) |
| CH-08 | 2026-09-21 | Як користувач, я хочу коректну форму блоку-метрики: помилка зникає одразу, як почав вводити текст, а заголовок форми відповідає дії (створення чи редагування). | 1. Починаю вводити текст у полі «Що рахуємо/вимірюємо» — помилка «Вкажіть, що рахуємо» гасне одразу (не чекає збереження). 2. Відкриваю редагування наявної метрики через олівчик — заголовок форми «Редагування» (не «Новий блок-метрика»). | виконано | `ui/MetricBlockForm.tsx` (`onChange` гасить `labelError`/`unitError`, заголовок за `initialValues !== undefined`) |
| CH-09 | 2026-09-21 | Як користувач, я хочу бачити всі картки архіву одразу, а не колодою, щоб порівнювати їх, не гортаючи по одній. | 1. Відкриваю «Архів карток». 2. Картки розкладені сіткою знизу вгору — той самий принцип, що базове розташування нових карток у «Схемі», не стос/колода. | виконано | `ui/ArchiveScreen.tsx` (CSS grid замість `DeckGrid`; `DeckGrid`/`DeckGridItem` лишаються лише для активної колоди, `DeckScreen.tsx`) |
| CH-10 | 2026-09-21 | Як користувач, я хочу 3 окремі режими картки замість 2, щоб напис «Постійний процес з метриками (без дати)» не позначав одночасно й режим картки, і чекбокс у формі блоку-метрики — та сама назва в двох місцях плутала. | 1. У «Режим картки» тепер 3 варіанти: «стан без вимірювань» (без змін) → «постійний процес» (форма нового/наявного блоку показує лише «Що рахуємо»+«Одиниця», без чекбокса/цілі/дати — картка вже сама каже, що процес постійний) → «з цілями та метриками» (форма лишається повною, як і був старий «metrics»). 2. Колишній `tracking_mode='metrics'` розведено на `'ongoing'`/`'goals'` — потребувало міграції бази (CHECK-обмеження дозволяло рівно 2 значення), існуючі картки перенесено в `'goals'` (найбільш сумісний вибір, нічого не втрачено). | виконано | Міграція `docs/features/life-area-card/migrations/10_expand_card_tracking_mode.{up,down}.sql` (промоучена `plan/app/migrations/1789981137560_expand-card-tracking-mode.sql`, застосована); `domain/card.ts` (`CardTrackingMode` 3 значення, `setTrackingModeNonState` — після review-fix, було двома окремими функціями), `infra/postgres-repo.ts`, `app/update-card.ts`, `src/app/main.tsx` (DTO), `ui/CardBack.tsx` (3-way radio), `ui/MetricBlockForm.tsx` (новий проп `mode`, ховає чекбокс/ціль/дату для `'ongoing'`), `contracts/openapi.yaml` |
| CH-11 | 2026-09-21 | Як користувач, я хочу бачити вибір режиму картки (CH-10) одразу при першому перегортанні картки БЕЗ жодної метрики, а не одразу кнопку «+ Додати блок-метрику» — крок вибору статусу зараз відсутній узагалі. | 1. Перегортаю картку без жодного блоку-метрики — «Режим картки» (3 варіанти) видно одразу, без меню «...». 2. Обрав «стан без вимірювань» — бачу лише мячики, нічого більше (секція метрик не рендериться взагалі). 3. Обрав «постійний процес»/«з цілями та метриками» — під вибором з'являється «+ Додати блок-метрику» з відповідним набором полів. 4. Коли в картки вже є хоч один блок — поведінка CH-07 лишається (режим ховається за меню «...»). | виконано | `ui/CardBack.tsx` (`hasNoMetrics`/`showTrackingModePicker`, EmptyState-напис прибрано — кнопка вже показує порожнечу). Дрібне: зона скролу звороту картки більше не наїжджає на кнопку «...» (правильний фікс — кнопка в звичайному потоці розмітки, не `absolute`; z-index на випадному меню, бо `z-10` загубився разом зі зміною). Review-fix (реальний баг): редагування наявного блоку тепер бере набір полів із ВЛАСНИХ налаштувань блоку, не з поточного режиму картки — інакше перемикання картки на «постійний процес» після того, як блок уже мав ціль/дату, тихо стирало б їх при звичайній зміні назви. Review-fix (ефективність): зміна режиму картки патчить дані локально, без повного перезапиту звороту |
| CH-12 | 2026-09-21 | Як користувач, я хочу, щоб перемикання картки на «постійний процес» одразу стирало ціль/дату з усіх наявних блоків-метрик (явний наслідок дії), а кнопка «Закрити» на звороті зникала в режимі «стан без вимірювань» (там і так нема куди «закривати» — вибір мячиків і є весь вміст); також редагування метрики має розгортатись прямо під нею в списку, і форма створення нової метрики повинна мати «На зад», щоб можна було вийти без збереження. | 1. Картка з блоком, що має ціль+дату, у режимі «з цілями» — перемикаю на «постійний процес»: усі блоки одразу втрачають ціль/дату (стають «постійний процес», без відновлення при поверненні назад — користувач вводить дати заново сам, якщо схоче). 2. Редагую блок після цього — форма вже без полів цілі/дати, нічого не «рятує» стару ціль мовчки. 3. У режимі «стан без вимірювань» після редагування (три крапки) кнопка «Закрити» не показується взагалі — з'являється знову лише для «постійний процес»/«з цілями та метриками». 4. Клік на олівець метрики розгортає панель редагування ПРЯМО під цим блоком у списку (не в кінці всього списку). 5. У формі створення нової метрики зліва від «Зберегти» — «На зад», що закриває форму без збереження. | виконано | `ui/CardBack.tsx` (`handleUpdateTracking`: при переході в `'ongoing'` — `Promise.all` циклом по `onUpdateMetricBlock` для кожного блоку, що ще не `isOngoing` — очищає ціль/дату, потім `refresh()` замість локального патча, бо дані блоків реально змінились; форма редагування блоку (`mode`) знов бере ПОТОЧНИЙ режим картки, не власні налаштування блоку — review-fix CH-10 знято, бо саме стирання тепер очікуване й відбувається одразу в момент перемикання, а не ховається до випадкового редагування; кнопка «Закрити» — `isEditingBack && trackingMode !== 'state'`; список блоків-метрик — панель редагування як другий елемент того самого `<div>`, що й `MetricBlockCard`, не окремим блоком у кінці), `ui/MetricBlockForm.tsx` (новий проп `onCancel` → кнопка «На зад» біля «Зберегти», лише коли проп переданий). **`/code-review` того самого раунду (13 знахідок, усі виправлено):** `handleUpdateTracking` розділяє помилку очищення блоків від помилки самого переходу режиму (не звинувачує режим, якщо перехід уже зберігся) і чекає `refreshAsync()` перед розблокуванням радіо; «+ Додати блок-метрику» й олівець редагування заблоковані на час переходу (`isSavingTracking`) — знімок блоків для очищення більше не «тікає» від нової метрики, створеної посеред переходу; `CardFace.saveEdit` більше не пересилає Опис, коли міняли лише Назву (`descriptionChanged`-guard); `startArchive` скидає `isEditingBack`; друга кнопка «Закрити» (панель редагування блоку) перейменована на «Закрити редагування» — уникнути двох однакових підписів на екрані водночас; меню «...» на звороті й на лиці картки тепер закривається кліком поза ним (той самий підхід, що вже мала шестерня верхнього бару); спільний `patchIfLoaded` (`ui/state-utils.ts`) прибирає повторений null-guard із `CardBack`/`CardFace`; `data-model.md` і `docs/ISSUES.md` (ISS-155/ISS-156) синхронізовано |
| CH-13 | 2026-09-21 | Як користувач, я хочу, щоб картки в колоді мали квадратну форму (верхня сторона дорівнює нижній) замість поточної прямокутної — так зручніше. | 1. Відкриваю «Картки» (колода). 2. Кожна картка — квадратна, не витягнута прямокутником. | виконано (не підтверджено живим тестуванням) | `plan/app/src/cards/life-area-card/ui/DeckGrid.tsx` — передня картка колоди: `h-[85%]` (висото-керований розмір, D-121) замінено на `aspect-square` (ширина = висота). **Живе тестування, 2 подальші правки:** (а) відступ пейджера ‹›/1-9 від кнопок "Створити картку"/"Архів карток" (Андрій: "збільш так щоб... як ці кнопки від нижнього бару") -- без розтягування картки на всю висоту flex-1 вільний простір лишався ПІД пейджером, роздуваючи цей відступ; `justify-end` на контейнері колоди притискає групу (картка+пейджер) до низу, відступ знову дорівнює тому самому `gap-3`, що й скрізь. (б) "навіть якщо зменшу масштаб картки мають відформатовуватись на весь екран" -- `max-w-xs` фіксував ширину в 20rem незалежно від розміру вікна; прибрано, розмір тепер керується ВИСОТОЮ (`h-full`, та сама ідея, що D-121, через `flex-1` предка), ширина обчислюється з неї через `aspect-square`, `max-w-full` -- запобіжник на вузькому/високому вікні |
| CH-14 | 2026-09-21 | Як користувач, я хочу, щоб блоки-метрики на звороті картки не вилазили горизонтальним скролом за межі картки і мали акуратні, непарні відступи між назвою й підписом. | 1. Відкриваю зворот картки з блоками-метриками. 2. Список блоків не показує горизонтальний скрол-бар. 3. Відступ між назвою метрики (наприклад «вф») і підписом під нею («постійний процес») — один чистий відступ, не подвоєний. | виконано (не підтверджено живим тестуванням) | `plan/app/src/cards/life-area-card/ui/CardBack.tsx` — той самий клас багу, що CH-09 (`structure/AnalyticsScreen.tsx`, м'ячик): `overflow-y-auto` каскадом вмикає й `overflow-x`, тож 6px-виступ кнопок "×"/"✎" (`MetricBlockCard.tsx`, навмисний негативний офсет) спричиняв горизонтальний скрол; `pr-2 pt-1` на контейнері — той самий запас, що CH-09. `plan/app/src/cards/life-area-card/ui/MetricBlockCard.tsx` — підписи (постійний процес/+X понад ціль/очікує перевірки) мали власний `mt-1` ПОВЕРХ `gap-1` контейнера (в flex margin і gap не схлопуються, відступ подвоювався до 8px) — прибрано, лишився один `gap-1` |
| CH-15 | 2026-09-21 | Як користувач, я хочу, щоб в Архіві карток назва картки була зверху зліва (як у колоді), зʼявилась кнопка "Видалити" (кошик), яка видаляє картку НАЗАВЖДИ, а самі картки завжди масштабувались, щоб уміщатись на екрані без скролу. | 1. Відкриваю «Архів карток» — назва кожної картки в лівому верхньому куті тайла (не по центру). 2. У правому верхньому куті тайла — кнопка-кошик. 3. Клік на кошик відкриває підтвердження з вводом слова «видалити» (та сама незворотна дія, що видалення блоку-метрики/акаунту) — лише після цього картка й усі її дані видаляються з бази назавжди. 4. Сітка тайлів завжди масштабується так, щоб усі картки архіву влізли на екран без скролу. 5. Кнопка "На зад" може перекривати тайли внизу — це прийнятно. | виконано (не підтверджено живим тестуванням) | Бекенд: `plan/app/src/cards/life-area-card/infra/postgres-repo.ts` (`deleteCard` — справжній `DELETE FROM card`, усі повʼязані таблиці мають `ON DELETE CASCADE` на `card_id` у міграціях, Postgres сам прибирає решту), `app/delete-card.ts` (use-case, дзеркало `restore-card.ts`: лише з архіву, `card.not_archived`/`card.not_found`), `ports/card-handlers.ts` (`deleteCard`), `server/app.ts` (`DELETE /api/v1/cards/{cardId}/permanent`). Фронтенд: `src/app/main.tsx` (`onDeleteCardPermanently`), `src/app/App.tsx` (проп прокинуто в `ArchiveScreen`), `ui/ArchiveScreen.tsx` (сітка з фіксованим розміром картки, `ConfirmDialog` з `requireTypedWord="видалити"`, `role="button"` замість `<button>` — вкладена кнопка "Видалити" всередині, `pb-20` прибрано з list-стану), `shared/ui/icons.tsx` (`TrashIcon`, новий значок у тому самому контурному стилі, що решта). Масштабування сітки — той самий принцип, що Схема (`structure` CH-15): `ResizeObserver` на видимій зоні, `transform: scale()` на сітці з ФІКСОВАНИМ розміром картки (не responsive breakpoints, як було). **Живе тестування, review-fix:** "корзинка десь не там де треба" -- картка мала `overflow-hidden` (успадковано зі старої кнопки, захищало довгі назви), яке обрізало кнопку "Видалити" -- та навмисно трохи виступає за край картки (`-right-1.5/-top-1.5`, той самий прийом, що `MetricBlockCard.tsx`). Прибрано `overflow-hidden` -- довгі назви й так переносяться (`break-words`) |
| CH-16 | 2026-09-21 | Як користувач, я хочу кнопку "+" по центру блоку-метрики (між назвою і показником), яка розгортає вниз форму "Додати/відняти показник" — вписую число, тисну "Додати" чи "Відняти" — щоб швидко зафіксувати зміну показника прямо з картки, без участі агента-чату. | 1. Бачу "+" по центру блоку (між назвою зліва і показником справа). 2. Клік розгортає під блоком форму з полем числа і двома кнопками "Додати"/"Відняти". 3. "Додати" — показник зростає на це число. 4. "Відняти" — зменшується на це число (технічно той самий запис, лише зі знаком мінус). 5. На самому блоці видно лише "+" — вибір додати/відняти лише всередині розгорнутої форми. ~~Уточнення (підтверджено): такий запис одразу зараховується в показник, без "очікує перевірки".~~ **Спростовано ревʼю коду (PR #18) — див. review-fix нижче: ця частина уточнення виявилась НЕПРАВИЛЬНОЮ, лишається видимою як історія, не переписується мовчки.** | виконано | `plan/app/src/cards/life-area-card/ui/MetricBlockCard.tsx` (кнопка "+" -- `grid-cols-[1fr_auto_1fr]`, `onQuickAdjust`, зелений тон `border-good/25 bg-good/10 text-good` за живим тестуванням "зроби кружечок плюсу зеленим"), `ui/CardBack.tsx` (`adjustingBlock`-стан, форма з `NumberField`, `handleQuickAdjust`, той самий toggle-принцип, що олівець CH-03), `ui/DeckFrontCard.tsx`/`ui/DeckScreen.tsx`/`src/app/App.tsx`/`main.tsx` (`onCreateEntry` прокинуто через усі шари, реальний виклик -- вже наявний POST `.../entries`, той самий, що пише агент). **Живе тестування, review-fix:** "+Додати блок-метрику" (кнопка створення НОВОГО блоку) закріплено на верхньому рядку біля "..." (той самий "закріпи, щоб не скролилось" принцип), а НЕ всередині зони, що скролиться зі списком метрик -- `MetricBlockForm` лишається інлайн у списку. **Ревʼю коду PR #18, реальний баг:** уточнення юзер-кейсу вище ("без очікує перевірки") виявилось НЕПРАВИЛЬНИМ -- AC-06 (spec.md §5, конфлікт близьких за часом записів ТОГО САМОГО блоку) лишається чинним і для цього каналу, тож два швидкі кліки поспіль на одній метриці й далі йдуть у "очікує перевірки" (обидва записи, і новий, і щойно confirmed попередній). Свідоме рішення НЕ обходити AC-06 для цього каналу -- [D-137](../../DECISIONS.md#d-137) (звужує [D-110](../../DECISIONS.md#d-110), який колись прибрав ручний запис заради чат-only). Замість обходу -- прозорість: `main.tsx`'s `onCreateEntry` повертає реальний `status`, `CardBack.tsx` показує `Banner` "Запис прийнято, але пішов на перевірку...", коли результат `pending`. Заразом виправлено регресію: закріплена кнопка "+ Додати блок-метрику" знову випереджала вибір режиму картки на порожній картці (той самий бар'єр, що CH-11 уже раз виправляв) -- тепер ховається в закріпленому рядку, поки блоків нема (`hasNoMetrics`), і рендериться ПІД пікером "Режим картки" замість цього. Дві кнопки з однаковим підписом "Закрити" одночасно на екрані -- перейменовано на "Закрити додавання" (той самий принцип, що вже "Закрити редагування", CH-12). `/simplify`: спільний `toggleBlockPanel`-хелпер (onEdit/onQuickAdjust дублювали toggle-логіку), `closeQuickAdjust`-хелпер (три різні місця вручну скидали підмножини тих самих трьох полів), `.finally()` замість дубльованого `setIsQuickAdjusting(false)`, спільний `RoundIconButton` у `MetricBlockCard.tsx` (три майже ідентичні кнопки-гурточки — "×"/"✎"/"+ — звелись до одного компонента). Відкриття однієї панелі блоку (редагування/швидке +/-) тепер закриває іншу, якщо вона була розкрита для того самого блоку |
| CH-17 | 2026-09-22 | Як користувач ПЛАН, я хочу бачити Опис картки одразу під назвою, а попередження агента про суперечність (AC-10) — нижче Опису, щоб головний зміст картки (навіщо вона) був першим, а технічна примітка — другорядною. | 1. Відкриваю картку, де є активна суперечність у даних. 2. Під назвою одразу бачу Опис. 3. Під Описом — окремим блоком попередження агента про суперечність. | виконано (не підтверджено живим тестуванням) | `plan/app/src/cards/life-area-card/ui/CardFace.tsx` — блок `data.dataWarning` (Banner) переставлено з позиції ПЕРЕД абзацом Опису на позицію ПІСЛЯ нього; текст коментаря над блоком доповнено |

## Пов'язане

- Скоординована зміна з [`structure` CH-01](../structure/changes.md) — та сама гілка, кнопка «Архів» переїжджає з Аналітики на Схему й дублюється тут.
- CH-02 скоординована з [`structure` CH-02](../structure/changes.md) — м'ячик стану на Схемі/Аналітиці.
- CH-05…CH-12 — знахідки живого тестування 2026-09-21 (гілка `life-plan-levels`), виконані на окремій гілці `life-area-card-live-fixes` (D-134) після мерджу `life-plan-levels` (PR #13) у `main`.
