---
status: done
---

# Review — life-area-card — 2026-09-07 (follow-up на review-fix хвилю T39-T52)

**Owner:** незалежне повторне рев'ю (`/sdd:review life-area-card`, клінтекст-агенти), клієнт-контекст: Андрій.
**Verdict: PASS** (усі stage-1 виправлені; stage-2 виправлені чи зафіксовані як відкладені з owner+due).

## Scope

Гілка `proof/sdd-life-area-card`, діапазон `eb3146e..b8719bf` (від коміту первинного рев'ю `review-2026-09-07.md` до завершення хвилі T39-T52).

- 76 файлів, +3425/-210 рядків.
- Перевіряє: чи реально виправлені 16 знахідок первинного рев'ю (`_review/review-2026-09-07.md`), не лише "тести зелені".

## Метод

Два незалежні клінтекст-рев'юери (`sdd:reviewer`), кожен читав spec/sad/screens/оригінальний review-запис сам:

1. **Backend** — A1-A3, B5-B9, group D (усі backend-фікси T39-T51).
2. **Frontend** — A4, C10-C16, group E (усі frontend-фікси T46-T52).

## Знахідки й резолюції

### Stage-1 (блокують, усі виправлені)

| # | Знахідка | Резолюція |
|---|---|---|
| B7-regression | Конфлікт-детекція (T44) трактувала "невідомий пристрій" як "можливо інший" — клієнт ніколи не надсилав sourceDeviceId, тож БУДЬ-ЯКІ два швидкі записи одного блоку завжди конфліктували, відкочуючи попередній підтверджений запис назад у "очікує" (AC-01 навпаки) | **Fix now** — `domain/conflict.ts` (unknown → "той самий пристрій"), `main.tsx` (реальний персистентний device id) — коміт `c8c89c8` |
| B5-remainder | 3 з 4 пар "запис картки + подія Літопису" (createCard/updateCard-markFilled/restoreCard) досі без транзакції — T40/T41 закрили лише archiveCard/transfer/createEntry | **Fix now** — `server/app.ts` (3 маршрути через `withTransaction`), доведено fault-injection проти реальної Neon — коміт `f6061d7` |
| C13-a | `loadCard` (лицьова сторона) взагалі без офлайн-кешу — офлайн падав у Banner без "перегорнути →", користувач не міг дістатись навіть до вже кешованого звороту | **Fix now** — `local-cache.ts` (readCachedCardFace/cacheCardFace) — коміт `683a756` |
| C13-b | Офлайн-фолбек ковтав і мережеву недоступність, і 401/404 — протермінована сесія показувала застарілу картку замість виходу | **Fix now** — `AppError`-розрізнення в `loadBack`/`loadCard`, ніколи не маскує статусну відповідь кешем — коміт `683a756` |
| AC-12-refresh | Невдалий фоновий рефреш після «виправити» (`handleFlagEntry`) trashив увесь екран — той самий клас, що T52 вже виправив для `refresh()`, пропущено тут | **Fix now** — той самий `refreshError`+requestId-патерн, guard подвійного кліку — коміт `cc9a4a5` |

### Stage-2 (якість, виправлені)

Referential-стабільність `onSessionExpired`/`onLogout` (App.tsx), C11-рецидив (кнопка "виправити" рендерилась і без onFlagEntry), `ConfirmDialog` aria-labelledby, нескінченний цикл у `collectAllPages` (реально відтворено — Node OOM), обрізана на 50 записів історія архіву, зіпсований/завеликий JSON → 500, відсутній рядок ISSUES.md для `entry.already_resolved`, застаріла документація `screens.md` — коміти `592f97c`, `5d1f9af`, `56116ad`.

### Stage-2 (якість, свідомо відкладені — ISS-88, ISS-90..94)

Non-UUID усередині транзакції (спроба фіксу зачепила 66 несуміжних тестів, потребує ширшого рішення на HTTP-межі); необмежений виклик Claude на кожне відкриття картки (потребує дизайн-рішення про throttling); `computeProgress` і далі кидає на вже отруєних (до T43) рядках, яких фізично не існує в цьому проєкті; клавіатурна доступність (редагування Опису, закриття тултипа); focus trap у `ConfirmDialog`; `clearAllCachedData` очищає весь origin, не лише цю фічу.

## Gate result

**PASS.** Усі 5 stage-1 знахідок виправлені й доведені тестами (юніт + інтеграційні проти реальної Neon, включно з fault-injection на атомарність). Stage-2 знахідки або виправлені, або зафіксовані в `docs/ISSUES.md` (ISS-88, ISS-90..94) з owner і умовою повернення — жодна не лишилась мовчки невирішеною.

Наступний крок — `/sdd:ship life-area-card`.
