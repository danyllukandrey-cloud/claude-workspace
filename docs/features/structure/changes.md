# Зміни — structure (CH-NN)

Процес і статуси — [CLAUDE.md §Правки в наявній фічі (CH-NN, D-133)](../../../CLAUDE.md).

| # | Дата | Юзер-сторі | Юзер-кейс | Статус | Де виконано |
|---|---|---|---|---|---|
| CH-01 | 2026-09-19 | Як користувач ПЛАН, я хочу, щоб сторінка «Аналітика» показувала картки зліва на всю висоту екрана, а загальний стан і звіти — праворуч, і щоб кнопка архіву карток лежала там, де я її інтуїтивно шукаю (на Схемі й на Картках), а не на Аналітиці — щоб швидше орієнтуватись і не втрачати місце під головні дані. | 1. Відкриваю «Аналітика» — зліва на всю висоту «Показники по картках» (без напису й цифр «ранг-розрив», прибрано з блоку), праворуч зверху «Загальний стан», під ним «Звіти» (вертикальна стрічка). 2. Плаваюча кнопка «Архів» знизу на цій сторінці зникає. 3. Переходжу на «Схема» — біля кнопки «Конфігурація» з'являється кнопка «Архів карток» (по центру), веде в той самий архів. 4. Результат: архів карток доступний з двох місць — Схема тут, Картки (координовано з `life-area-card` CH-01). | виконано | `plan/app/src/structure/ui/AnalyticsScreen.tsx` (переверстано, кнопку "Архів" прибрано), `plan/app/src/structure/ui/LayoutBoard.tsx` (кнопка "Архів карток" біля "Конфігурація"), `plan/app/src/app/App.tsx` (спільний callback `openArchive`, app-composition) |

| CH-02 | 2026-09-19 | Як користувач ПЛАН, я хочу бачити кольоровий м'ячик стану картки (координовано з `life-area-card` CH-02) також на Схемі й Аналітиці, а не лише на самій картці, щоб мати повну картину пріоритетів в одному місці. | 1. Картка отримує нове поле стану («стан без вимірювань») — `life-area-card` CH-02. 2. Схема (`LayoutBoard.tsx`) і Аналітика (`AnalyticsScreen.tsx`) НЕ перевикористовують вигляд картки з `life-area-card` — малюють власну спрощену версію (`LayoutBoardCard`/`AnalyticsScreenCard`, лише текстові поля через власний канал завантаження `loadLayout`/`loadAnalytics`). 3. Нове поле стану прокидається через цей самий канал і домальовується м'ячиком у `cardChip` (`LayoutBoard.tsx:478`) і в картці показників (`AnalyticsScreen.tsx:200`). 4. Результат: та сама кольорова індикація видна на картці, в колоді і на Схемі/Аналітиці одночасно. | виконано | `ui/LayoutBoard.tsx` (`LayoutBoardCardHealthState`, м'ячик у `cardChip`, перекладений `aria-label`), `ui/AnalyticsScreen.tsx` (`AnalyticsScreenCardHealthState`, м'ячик у картці показників), `index.ts` (експорт нових типів), `src/app/main.tsx` (`loadLayout`/`loadAnalytics` — join через `fetchCardSummaries`); джерело поля — `life-area-card` CH-02 (`domain/card.ts`, `CardDto.trackingMode`/`healthState`) |

| CH-03 | 2026-09-21 | Як користувач, я хочу кнопку «На зад» на сторінці конфігурації Схеми і правильне розташування «На зад» в Архіві карток. | 1. Сторінка конфігурації Схеми (вибір розкладки) — «На зад» зліва від «Зберегти». 2. «Архів карток» (відкритий зі Схеми чи з Карток) — кнопка «На зад» знизу по центру. | виконано (частина 1 з 2 — частина 2, Архів карток, виконується в окремій гілці `life-area-card`) | `plan/app/src/structure/ui/LayoutBoard.tsx` (CONFIG-екран: кнопка "На зад" зліва від "Зберегти", той самий патерн, що `life-area-card`'s `MetricBlockForm.onCancel` — закриває CONFIG без збереження, скидає `pendingLayoutMode`) |
| CH-04 | 2026-09-21 | Як користувач, я хочу, щоб базове положення карток на Схемі не ховалось за краєм екрана і давало явний вибір «визначено / невизначено». | 1. Коли на Схемі з'являється нова картка в базовому стані знизу, схема автоматично перемасштабовується так, щоб зона внизу не затуляла решту розкладки. 2. Перетягнув картку в затемнену зону внизу і лишив — картка в стані «невизначено». 3. Перетягнув назад на схему — картка «визначена» на конкретній позиції. | виконано | `plan/app/src/structure/ui/LayoutBoard.tsx`: п.1 — `canvasScaleY()`/`trayRef` стискають відображення (не збережені дані) карток канви по Y, коли купка видима, щоб вона їх не затуляла. П.2/3 — купка тепер рендериться як явна ціль ще ДО того, як у ній щось лежить (`showTray`, підсвічується під час будь-якого драгу), `handleUp` розпізнає відпускання в межах прямокутника купки (`trayRef`) і позначає картку "невизначеною" локально (`pendingUnassignedIds`), а не викликає `onMoveCard`; перетягування назад на канву знову викликає `onMoveCard` і прибирає позначку. **Відома межа** (задокументовано [ISS-157](../../ISSUES.md)): "невизначено" через перетягування — клієнтський, ефемерний стан у межах одного відкриття Схеми (`PUT /structure/layout/{cardId}` приймає лише число, не `null` — розширення контракту поза межами цієї правки). **`/code-review` review-fix (реальний баг):** `pendingUnassignedIds` скидалась лише при переході САМЕ в staging — перемикання з staging на БУДЬ-ЯКИЙ реальний режим (чи ручне перетягування картки в трей, потім зміна конфігурації) лишало картки "застряглими" в треї, хоча сервер уже дав їм справжні позиції; тепер скидається на порожню множину для будь-якого іншого режиму. Заодно: плавний CSS-перехід (`transition-all duration-150`) для карток, що НЕ тягнуться зараз, — ховає видимий "стрибок" картки в момент, коли трей ховається одразу після drop (компресія канви по Y тимчасово вмикається на ввесь час будь-якого драгу, не лише переносу в трей) |
| CH-05 | 2026-09-21 | Як користувач, я хочу, щоб дія «Закрити напрямок» на картці Схеми стала «Архівувати» з тим самим механізмом, що в колоді — без мережевих помилок і без дублювання картки. | 1. Три крапки на картці Схеми → «Архівувати» (не «Закрити напрямок» — прибрано). 2. Той самий архівний флоу, що з колоди карток (CH-06 нижче). 3. Немає більше стану «зникла зі Схеми, лишилась у колоді, з'явилась знову внизу при поверненні на вкладку». | виконано | `plan/app/src/structure/ui/LayoutBoard.tsx` (дія на чипі — "Архівувати" замість "Закрити напрямок", новий проп `onArchiveCard`/`onTransferMetricBlock` замість структуроспецифічного `onCloseCard`), `plan/app/src/app/App.tsx` (`onArchiveCard={archiveCard}`, `onTransferMetricBlock={onTransferMetricBlock}` — ТІ САМІ injected callbacks, що вже архівують з колоди, D-103 сам закриває позицію в Структурі), `plan/app/src/app/main.tsx` (структуроспецифічний `onCloseCard`/POST `.../close` прибрано повністю) |
| CH-06 | 2026-09-21 | Як користувач, я хочу зрозумілу сторінку підтвердження архівування з можливістю перенести метрики на іншу картку перед архівацією. | 1. Заголовок сторінки — «Архівування» (не «Закрити «Назва картки»»). 2. Список усіх метрик картки, кожна в рамці з чекбоксом і кнопкою «Перенести» (з'являється після вибору картки-цілі). 3. Обрав метрику чекбоксом → обрав картку, куди перенести → «Перенести». 4. Зняв чекбокс — форма скидається до вибору картки. 5. «Архівувати без перенесення» знизу — архівує без маніпуляцій з метриками. | виконано | `plan/app/src/structure/ui/CloseCardDialog.tsx` перейменовано на `plan/app/src/structure/ui/ArchiveCardDialog.tsx`, потім ще раз на `LayoutBoardArchiveDialog.tsx` (`/code-review`: перша назва збігалась дослівно з іншим, функціонально відмінним компонентом `life-area-card/ui/ArchiveCardDialog.tsx`) — заголовок "Архівування" рендерить `LayoutBoard.tsx`, картка-підзаголовок окремо — кожен рядок метрики переноситься ОДРАЗУ по кліку "Перенести" (той самий патерн, що `life-area-card`'s `CardBack.tsx` "Перенести на іншу картку"), не пакетно перед архівацією; "Архівувати без перенесення" викликає лише `onArchive`, без жодної метрик-логіки. **`/code-review` review-fix (реальний баг):** чекбокс і "Архівувати без перенесення" тепер заблоковані, поки перенесення метрики цього рядка ще в польоті — раніше можна було зняти чекбокс чи архівувати картку одночасно з запитом перенесення, що вже летів |
| — | 2026-09-21 | **Перевірка (не CH, DoD для CH-04+CH-05+CH-06):** після виконання — архівована картка не з'являється повторно на Схемі в жодному положенні після переходу на іншу вкладку і назад; зникає з колоди так само, як зі Схеми; розархівація коректно повертає картку і в колоду, і в базове (невизначене) положення Схеми. | Живе тестування 2026-09-21, п. 11. | виконано (для CH-05/CH-06 — архівація тепер іде через ТОЙ САМИЙ `archiveCard`, що вже закриває позицію в Структурі, D-103; окремого структуроспецифічного шляху, що міг розійтись зі станом колоди, більше нема) | — |
| CH-07 | 2026-09-21 | Як користувач, я хочу коректні показники на Аналітиці для карток без %-цілі (стан/лічильник/мʼячик статусу), щоб кожен блок показував реальний статус, а не оманливий 0% чи порожнечу. | 1. Картки без обчислюваного відсотка (напр. «Побут», «Філософія») показують кількість дій/разів, не «0%». 2. Метрика «Перегляд» картки «Філософія» відображається на Аналітиці (зараз ніяк не враховується). 3. Картки типу «стан без вимірювань» (напр. «Кар'єра») показують той самий кольоровий мʼячик статусу (зелений/червоний/жовтий), що вже видно на самій картці й на Схемі (`life-area-card`/`structure` CH-02) — зараз на Аналітиці його не видно взагалі. 4. Результат: кожен блок на Аналітиці має або відсоток/лічильник, або мʼячик статусу — жоден не лишається порожнім. | виконано | Нове поле `AnalyticsScreenCard.actionCount` (`plan/app/src/structure/ui/AnalyticsScreen.tsx`) — сума confirmed-записів картки, рендериться замість відсотка, коли `progress===null` і `healthState===null`; `formatActionCount` — українська форма множини ("1 раз"/"2 рази"/"5 разів"). Джерело даних — новий `fetchCardActionCount` (`plan/app/src/app/main.tsx`, GET `.../entries` по всіх сторінках, лише для карток без bounded-блоку і без trackingMode==='state' — `CardDetailDto` не несе список блоків, ISS-39). П.3 (мʼячик стану) уже був реалізований CH-02 — перевірено робочим, без змін. |
| CH-08 | 2026-09-21 | Як користувач, я хочу зрозумілу й передбачувану сторінку Декларації, де видно, коли текст справді збережеться, а коли ні. | 1. Заголовок форми зміни декларації — «Опишіть Вашу картину світу, як і ким Ви себе відчуваєте, або який шлях вибрали». 2. Кнопка «Змінити декларацію» → «Декларувати». 3. Зліва від «Декларувати» — «На зад»: повертає на лицеву сторінку без збереження тексту. 4. Зберігає й показує текст лише клік на «Декларувати». | виконано | `plan/app/src/structure/ui/DeclarationScreen.tsx` (textarea редагує окремий стан `draft`, не сам збережений `declaration`; підпис кнопки збереження — «Змінити декларацію» у VIEW / «Декларувати» у EDIT; нова кнопка «На зад» зліва від неї в тому самому floating-блоці, лише в EDIT, скидає `mode` в `'view'` без `persist()` і без збереження чернетки; новий заголовок textarea), `plan/app/src/structure/ui/DeclarationScreen.test.tsx` (тести на обидва підписи кнопки, відсутність «На зад» у VIEW, відкидання чернетки «На зад», новий заголовок textarea). **`/code-review` review-fix (реальний баг):** `isSaving`-гейт на обидві кнопки — без нього клік «На зад» ПІД ЧАС того, як `onSave` ще в польоті, перемикав екран на VIEW зі старим текстом, а щойно запит резолвився, `persist()` мовчки перезаписував текст чернеткою й показував «Збережено» на екрані, який користувач щойно «скасував» |
| CH-09 | 2026-09-21 | Як користувач, я хочу, щоб кольоровий мʼячик статусу картки на Аналітиці показувався повністю, а не наполовину обрізаний/схований, щоб колір статусу було видно чітко. | 1. Відкриваю Аналітику. 2. Дивлюсь на картку показників картки типу «стан без вимірювань». 3. Мʼячик статусу видний цілком, не зрізаний краєм контейнера чи сусіднім елементом. | виконано | `plan/app/src/structure/ui/AnalyticsScreen.tsx` — гіпотеза підтвердилась: `overflow-y-auto` на прокручуваному контейнері (без явного `overflow-x`) CSS-каскадом обчислює й `overflow-x` як `auto`, тож негативний offset мʼячика (`-right-1 -top-1`, chip-gloss) обрізало для карток першого рядка/останнього стовпчика сітки. Фікс — не рухати сам мʼячик (лишився б зі зсувом відносно решти карток), а додати контейнеру `pt-1 pr-1` — точний запас під той самий offset. |
| CH-10 | 2026-09-21 | Як користувач, я хочу, щоб перемикання конфігурації розкладки Схеми на режим «Готово до розкладання» автоматично переносило всі картки, що вже лежать на канві, назад у список знизу, щоб я міг розкласти їх по-новому. | 1. Відкриваю «Конфігурація» Схеми — картки вже розташовані на канві (мають визначену позицію). 2. Перемикаю режим на «Готово до розкладання». 3. Усі картки з канви автоматично переїжджають у список знизу («перетягни картки знизу на канву») — канва порожня. 4. Заново перетягую потрібні картки на канву в новому порядку. | виконано | `plan/app/src/structure/ui/LayoutBoard.tsx`'s `persistLayoutMode`: після успішного збереження `layoutMode: 'staging'` і перечитування розкладки (сервер для staging нічого не пише, `domain/layout.ts`'s `computeStagingLayout` — явний no-op, `apply-layout-mode.ts:51`) — усі картки з `x !== null` позначаються "невизначеними" тим самим клієнтським `pendingUnassignedIds`, що CH-04. Та сама відома межа з ISS-157: ефемерно в межах одного відкриття Схеми, не персистентно на сервері |
| CH-11 | 2026-09-21 | Як користувач, я хочу, щоб вибір інструменту «Лінія»/«Стрілка» на Схемі скасовувався не лише повторним натисканням тієї самої кнопки, а й правим кліком будь-де на екрані Схеми (крім самих кнопок інструменту). | 1. Натискаю «Лінія» чи «Стрілка» — інструмент активний. 2. Клацаю правою кнопкою миші будь-де на екрані Схеми (не на кнопках інструменту). 3. Інструмент скасовується, повертаюсь у звичайний режим — так само, як повторний клік на ту саму кнопку інструменту. | виконано (не підтверджено живим тестуванням) | `plan/app/src/structure/ui/LayoutBoard.tsx` — `window.addEventListener('contextmenu', ...)`, активний лише поки `linkTool !== null`, `preventDefault` ховає системне контекстне меню |
| CH-12 | 2026-09-21 | Як користувач, я хочу підтвердження перед видаленням звʼязку (лінії/стрілки) між картками на Схемі, щоб випадковий клік по лінії не видаляв її одразу. | 1. У звичайному режимі (без активного інструменту «Зв'язати») клацаю на наявну лінію/стрілку. 2. Замість миттєвого видалення з'являється кнопка підтвердження «Видалити». 3. Клацаю «Видалити» — звʼязок зникає. 4. Клік деінде без підтвердження скасовує запит на видалення, звʼязок лишається. | виконано (не підтверджено живим тестуванням) | `plan/app/src/structure/ui/LayoutBoard.tsx` — `pendingDeleteConnectionId`: клік на лінію озброює (`handleConnectionClick`, `stopPropagation`), кнопка «Видалити» рендериться біля середини лінії й викликає справжнє видалення, клік на канву деінде скидає озброєний стан; `LayoutBoard.test.tsx` — 3 нові тести (озброєння, підтвердження, скасування) |
| CH-13 | 2026-09-21 | Як користувач, я хочу, щоб коли картка зникає з канви Схеми (перетягнута в трей нерозкладених, чи режим розкладки перемкнутий на «Готово до розкладання»), усі її лінії/стрілки зʼязку теж зникали з екрана — а не висіли, впираючись у порожнє місце, де картка була раніше. | 1. На Схемі є картка «А», з'єднана лінією з карткою «Б». 2. Перетягую «А» в трей нерозкладених (чи перемикаю конфігурацію на «Готово до розкладання», яка робить те саме для всіх карток одразу) — «А» зникає з канви. 3. Лінія до «А» теж зникає з канви, не лишається висіти в порожньому місці. 4. Повернув «А» назад на канву — той самий звʼязок з'являється знову (лінія лише ПРИХОВУЄТЬСЯ, поки картка в треї, не видаляється насправді). | виконано (не підтверджено живим тестуванням) | `plan/app/src/structure/ui/LayoutBoard.tsx` — рендер лінії зв'язку тепер перевіряє `isEffectivelyUnassigned` (той самий предикат, що вже ховає чип картки з канви, CH-04) для ОБОХ кінців, не лише `card.x === null`; раніше `renderedPosition` враховував лише серверну відсутність позиції, не клієнтський трей-стан (`pendingUnassignedIds`); `LayoutBoard.test.tsx` — новий регресійний тест |
| CH-14 | 2026-09-21 | Як користувач, я хочу, щоб банер помилки на Схемі («Не вдалося зберегти позицію») зникав сам, коли я знову успішно перетягую картку, а не лишався висіти на екрані назавжди після одного невдалого перетягування. | 1. Перетягування картки провалилось мережевою помилкою — банер показав текст помилки. 2. Наступного разу перетягую (будь-яку) картку знову. 3. Старий банер зникає одразу на початку нового перетягування (не чекаючи результату нового запиту) — якщо новий запит теж провалиться, з'явиться свіжий банер з актуальним текстом. | виконано (не підтверджено живим тестуванням) | `plan/app/src/structure/ui/LayoutBoard.tsx`'s `handlePointerDown` — `setBanner(null)` на самому початку; раніше банер скидався лише в ОДНОМУ місці (`dismissArchiveDialog`), `Banner.tsx` навмисно без кнопки "закрити" (design-system.md), тож помилка з давно вирішеної проблеми (напр. Філософія до архів/відновити циклу) виглядала як актуальна; `LayoutBoard.test.tsx` — новий регресійний тест |
| CH-15 | 2026-09-21 | Як користувач, я хочу, щоб уся сцена Схеми (картки + лінії) масштабувалась як одне ціле, коли доступне місце полотна стає затісним (більший масштаб браузера/вужче вікно), щоб картки не накладались одна на одну — і не ставали більшими за звичний розмір, коли місця багато. | 1. Збільшую масштаб браузера (чи звужую вікно) — доступне місце полотна зменшується. 2. Замість того щоб картки лишались тим самим розміром і починали накладатись, уся сцена (картки+лінії) пропорційно зменшується — відносне розташування лишається тим самим. 3. Є нижня межа зменшення (сцена не стає нечитабельно дрібною). 4. Коли доступного місця багато (широке вікно/зменшений масштаб) — картки не стають більшими за звичайний розмір. 5. Зона нерозкладених карток і тонка риска-розділювач унизу лишаються стабільними (над кнопками, та сама ширина), НЕ частина сцени, що масштабується. | виконано (не підтверджено живим тестуванням) | `plan/app/src/structure/ui/LayoutBoard.tsx` — внутрішній `contentRef`-контейнер, уся система відсотків (0-100) рахується відносно НЬОГО. **ІТЕРАЦІЯ 2 (живе тестування 125%/159%/зменшеного масштабу знайшло регресії):** (а) перша версія мала жорстко закодований "еталонний" розмір (900x480) — на реальних екранах, де канва природно НІКОЛИ не сягала такого розміру, це змушувало сцену стискатись завжди, навіть на звичайному масштабі, і спричиняло видимі глюки (текст назв зникав, трей "розлітався"). Замінено на самокалібрування — `referenceSizeRef` (не `useState`, щоб ResizeObserver-колбек завжди читав свіже значення) фіксує РЕАЛЬНИЙ розмір `canvasRef` при першому вимірюванні, "масштаб=1" відтоді означає саме цей розмір, жодних магічних чисел. (б) трей повернуто ПОЗА `contentRef` (не частина масштабованої сцени) — "зона базової розкладки має залишатись над кнопками", а не "летіти" разом зі стисненою сценою; `canvasScaleY`/`rectPullback` і далі рахують проти `contentRef` (getBoundingClientRect на трансформованому елементі коректно повертає вже візуальний прямокутник, порівняння двох реальних viewport-прямокутників лишається коректним незалежно від вкладеності). (в) трей: `inset-x-0` → `inset-x-10` (та сама ширина, що риска-розділювач, "по ширині як промінь"), прибрано `min-h-[3.5rem]` (висота тепер природна, під реальний вміст чипів, не довільна константа — логіка розміру самих чипів навмисно поза цією правкою). jsdom (тести) не має `ResizeObserver` — feature-detect, `canvasScale` лишається 1 в тестах |
| CH-16 | 2026-09-21 | Як користувач, я хочу, щоб картки на Схемі стояли РІВНО там, де я їх фізично поставив, і ніколи не переміщувались автоматично через якесь "форматування" (напр. щоб трей не затуляв картки внизу) — єдине правило зміни позиції картки: я сам її туди переніс. | 1. Захоплюю картку мишкою — жодна ІНША картка на канві не рухається (раніше весь час захоплення трей ставав видимим і стискав усі картки вгору, навіть до першого руху курсора). 2. Перетягую картку з купки нерозкладених на канву, відпускаю ближче до низу — картка лишається РІВНО там, де відпустив, а не "відскакує" вище через стиснення відносно трею. 3. Це скасовує CH-04 п.1 (авто-стиснення карток по Y, коли видимий трей) повністю — трей просто накладається на канву як є, без жодного авто-перерозташування чужих карток. | виконано (не підтверджено живим тестуванням) | `plan/app/src/structure/ui/LayoutBoard.tsx` — `canvasScaleY()` (CH-04 п.1) видалено повністю; `renderedPosition` більше не приймає `scaleY`, повертає `card.x`/`card.y` як є, без множення. Трей і далі візуально може накладатись на картку в самому низу канви (свідомий компроміс -- "єдине правило" явно це дозволяє). `LayoutBoard.test.tsx` — стара точна перевірка стиснення замінена на перевірку "позиція НЕ стискається", плюс новий регресійний тест відтворює сценарій "відпустив біля низу, поки трей ще видимий (друга картка нерозкладена)" |

## Пов'язане

- Скоординована зміна з [`life-area-card` CH-01](../life-area-card/changes.md) — та сама гілка, дубль кнопки «Архів карток» на сторінці Картки.
- CH-02 скоординована з [`life-area-card` CH-02](../life-area-card/changes.md) — джерело поля стану картки.
- Показники для карток без жодного метрик-блоку (напр. Побут/Філософія) — досліджувалось окремо, тепер закривається CH-07.
- CH-03 (частина 2, Архів карток) скоординована з [`life-area-card`](../life-area-card/changes.md) — той самий спільний `ArchiveScreen.tsx`, що живе у фічі `life-area-card`, тут не редагувався (D-134).
- CH-03…CH-10 — знахідки живого тестування 2026-09-21, виконані на гілці `structure-live-fixes` (3 паралельні агенти, кожен в ізольованій worktree: Схема, Аналітика, Декларація) після мерджу `life-area-card-live-fixes` (PR #14) у `main`.
