# Review — life-plan-levels (2026-09-20)

## Обсяг

Гілка `life-plan-levels` супроти `main`: 37 файлів, +4606/-28 рядків. Повний ланцюг `specify → clarify → design → sequences → data-model → api → tasks → implement`, комітований на гілку окремо від `main` (комміти T1–T14 з мітками `SDD-Task`/`SDD-AC`, плюс мій самостійний фікс AC-04 `9218394`).

## Процес

Два незалежні рев'юер-агенти (`subagent_type: "sdd:reviewer"`, чистий контекст, без доступу до попередньої розмови) запущені паралельно над усім діфом. Кожен читав `spec.md` §5, `sad.md` §6, `data-model.md`, `contracts/openapi.yaml`, `tasks.json` самостійно — не з переказу. Знахідки зведені; 3 з 7 підтверджені обома агентами незалежно (сильніший сигнал довіри).

## Stage 1 — трасування AC (spec → sequences → data-model → api → tasks → implement)

Усі 8 user stories §4 і всі 11 AC (`AC-01…AC-09`, `AC-03b`, `AC-11`) простежені наскрізно через кожен шар. Жоден user story чи AC не випав з ланцюга. Ціль `target_surfaces: [backend-service, web-frontend]` покрита обома поверхнями — є і backend-тест, і UI-тест на кожен AC, що це вимагає.

## Stage 2 — знахідки

| # | Знахідка | Файл | Підтверджено обома? | Вердикт |
|---|---|---|---|---|
| A | POST/PATCH/DELETE пунктів плану не обгорнуті в `withTransaction` — збій запису в Лог дій міг лишити пункт плану напівзбереженим (sad.md §8, AC-05) | `server/app.ts` | так | Виправити зараз |
| B | Збій збереження пункту, підтвердженого в чаті, губився мовчки — користувач не бачив жодної відповіді | `src/agent/app/handle-message.ts` | ні | Виправити зараз |
| C | Сторінка ПЛАН не перечитує список після підтвердження пункту в чаті, поки сторінка відкрита (AC-09) | `src/app/App.tsx` | так | Виправити зараз |
| D | Сторінка ПЛАН не мала станів loading/error/retry — невдале завантаження лишало спінер назавжди | `src/plan-horizons/ui/PlanScreen.tsx` | так | Виправити зараз |
| F | Дата додавання показує рік лише для стратегічного горизонту, хоча той самий edge case можливий для будь-якого | `src/plan-horizons/ui/PlanScreen.tsx` | ні | Виправити зараз |
| G | Кнопка "+" — власна розмітка `<button>` замість спільного компонента `IconButton` | `src/plan-horizons/ui/PlanScreen.tsx` | ні | Виправити зараз (Андрій свідомо обрав це замість "залишити як є") |
| E | Контракт не документував два коди помилок, які код уже кидав (`plan_item.horizon_invalid`, `plan_item.nothing_to_update`) | `contracts/openapi.yaml` | ні | Виправити зараз |

Усі 7 знахідок вирішено з Андрієм через прямі запитання — усі сім разів обрано "Виправити зараз", жодного відкладеного чи відхиленого пункту.

## Виправлення

- Комміт `928cd86` — знахідки A, B, C, D, F, G (код + тести).
- Комміт `29f0d67` — знахідка E (контракт + `api-sync-report.md`).

Кожне виправлення пройшло TDD-дисципліну (тест спершу підтвердив проблему, потім фікс). Після всіх фіксів — один повний прогін усього набору тестів разом (не лише по одному фіксу): 1224 юніт-тести + 96 інтеграційних — усі зелені; лінт (`tsc --noEmit` для фронтенду й сервера) чистий. Дві інтеграційні перевірки (`T16`, `T17` з іншої фічі `life-area-card`) один раз таймаутнули при повному прогоні через мережеву затримку до реальної Neon — підтверджено як флакі: в ізоляції обидві проходять за <1с.

## Вердикт

**PASS.** Жодної відкритої знахідки рівня AC не лишилось. Готово до `/sdd:ship`.
