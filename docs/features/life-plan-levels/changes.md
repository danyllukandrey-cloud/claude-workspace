# Зміни — life-plan-levels (CH-NN)

Процес і статуси — [CLAUDE.md §Правки в наявній фічі (CH-NN, D-133)](../../../CLAUDE.md).

| # | Дата | Юзер-сторі | Юзер-кейс | Статус | Де виконано |
|---|---|---|---|---|---|
| CH-01 | 2026-09-21 | Як користувач ПЛАН, я хочу кнопку «На зад» у редакторі пункту плану, щоб мати вихід без збереження — навіть коли ще не ввів текст нового пункту. | 1. Натискаю «+» у горизонті — відкривається редактор нового пункту. 2. Не вводжу текст (або передумав, є текст). 3. Ліворуч від «Зберегти» бачу «На зад». 4. Клік — редактор закривається без збереження, незалежно від того, порожнє поле чи ні (той самий жест і для наявного пункту — залишає його без змін). | виконано | `plan/app/src/plan-horizons/ui/PlanItemEditor.tsx` (кнопка «На зад», `onClose` без збереження), тести CH-01 у `PlanItemEditor.test.tsx` |
| CH-02 | 2026-09-21 | Як користувач ПЛАН, я хочу явну кнопку «Видалити» на пункті плану, що вже існує, щоб не здогадуватись про приховану дію «стерти весь текст і зберегти» (AC-04 `spec.md`). | 1. Відкриваю наявний пункт плану (клік по тексту). 2. Праворуч від «Зберегти» бачу «Видалити» — лише для наявного пункту, не для нового. 3. Клік — пункт видаляється (той самий `onDelete`, що вже стоїть за жестом «очистити текст», AC-04), редактор закривається. 4. Стара поведінка (очистити текст → «Зберегти» = видалення) лишається робочою — нова кнопка не скасовує AC-04, а робить дію очевидною. | виконано | `plan/app/src/plan-horizons/ui/PlanItemEditor.tsx` (`handleDelete`, кнопка «Видалити» лише для `target.kind === 'existing'`), тести CH-02 у `PlanItemEditor.test.tsx` |
| CH-03 | 2026-09-21 | Як користувач ПЛАН, я хочу бачити значок-олівчик біля тексту пункту плану, щоб розуміти, що пункт можна редагувати, не здогадуючись, що для цього треба клікнути саме на текст. | 1. Відкриваю «ПЛАН». 2. Поруч із текстом кожного пункту (в кінці рядка, після дати додавання) бачу маленький олівчик. 3. Клік на олівчик — так само, як і клік на сам текст (лишається робочим) — відкриває редактор цього пункту. | виконано | `plan/app/src/plan-horizons/ui/PlanScreen.tsx` (гліф «✎» у кінці рядка, той самий, що олівець `MetricBlockCard.tsx`), тест CH-03 у `PlanScreen.test.tsx` |
