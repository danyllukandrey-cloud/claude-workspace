# ISSUES — реєстр виявлених нестиковок проєкту «ПЛАН»

Одне місце, куди записуємо кожну знайдену **нестиковку логіки** — розбіжність між тим, що зроблено, що записано в документах, і що заплановано. Мета — не загубити знахідку, щоб пізніше свідомо пошукати рішення, а не натикатись на ту саму проблему повторно як на нову.

## Як цим користуватись

**Це журнал знахідок, а не журнал рішень.** Знайшов нестиковку → допиши рядок сюди одразу, навіть якщо рішення ще нема. Знайшов рішення → онови статус тут, а саме рішення (якщо воно рівня D-NN за критерієм із CLAUDE.md) запиши в `docs/DECISIONS.md` і зв'яжи посиланням. Не видаляй закриті рядки — вони показують, що вже траплялось і як вирішилось.

**Що вважається нестиковкою (пишемо сюди):**
- Документ каже одне, а реальний стан файлів/коду — інше.
- Два документи описують той самий факт по-різному.
- Показник (%, статус фази) не відповідає тому, що видно по чек-листах чи Git-історії.
- Логічна помилка чи прогалина, яку хтось (людина, `pm`, будь-який агент) помітив під час перевірки, але вона не виправляється негайно тут і зараз.

**Що НЕ пишемо сюди:** дрібне форматування, одруківки — їх просто виправляють на місці без запису.

**Формат рядка:** одна нестиковка — один рядок реєстру. Номер `ISS-NN`, наступний вільний — рахувати максимум:

```powershell
Select-String docs\ISSUES.md -Pattern '^\| ISS-(\d+) \|' | ForEach-Object { [int]$_.Matches.Groups[1].Value } | Sort-Object | Select-Object -Last 1
```

## Статуси

| Статус | Значення |
|---|---|
| **відкрито** | знайдено, рішення ще не шукали |
| **у роботі** | рішення обговорюється чи перевіряється |
| **закрито** | вирішено — див. колонку «Рішення / посилання» |

---

## Реєстр

| # | Дата | Де знайдено | Що не збігається | Статус | Рішення / посилання |
|---|---|---|---|---|---|
| ISS-1 | 2026-08-28 | `docs/DELIVERY-PLAN.md` §«Критичні блокери» | Таблиця каже «наразі немає», а текст фази «Розробка» нижче прямо називає блокер — очікування бекенд-середовища (D-12) | закрито | 2026-08-29: таблицю виправлено — блокер (Docker/D-12) записано в неї явно; узгоджено з рештою файлу |
| ISS-2 | 2026-08-28 | Список карток-підказок для старту (`Concept.md` крок 8) — повторювався в `PRODUCT-BOOK.md`, `DELIVERY-PLAN.md`, `PRODUCT-BOOK_OWNER-QUESTIONNAIRE.md` | «Медитація» стояла в одному ряду з п'ятьма крупними зонами життя, хоча за суттю це практика/метрика всередині більшої зони, не сама окрема зона | закрито | замінено на «Стосунки» (уже вживане в `life-area-card` як приклад декларативної картки без метрики) в усіх трьох файлах + в `docs/Product_Overview/Concept.md` |
| ISS-3 | 2026-08-28 | `PRODUCT-BOOK_OWNER-QUESTIONNAIRE.md` §«Список відкритих питань», рядки #4, #5, #8 | Таблиця показувала «відкрито»/«потребує роботи» для трьох питань, які вже отримали відповідь в інших документах (`agent/spec.md §8` ADR-0004 для #5, `Concept.md` §1-2 для #8, `Concept.md` §3 для #4) — таблиця не синхронізувалась після появи відповідей | закрито | рядки #4 і #8 позначено закритими; #5 лишено частково відкритим — обов'язкова звірка механізму із зовнішнім курсом «агентів воркфлоу» ще не проведена |
| ISS-4 | 2026-08-28 | `docs/DELIVERY-PLAN.md` рядок 471 vs рядок 228 (той самий файл) | Документ суперечив сам собі: одне місце (2026-08-27) казало «`Concept.md` ще не створено», інше (додане того ж дня комітом `7e7151e`) — «написаний повністю» | закрито | рядок 471 переписано, і додано «Виконано 2026-08-28» під КОНЦ-1 |
| ISS-5 | 2026-08-28 | `docs/DELIVERY-PLAN.md` рядок 36 (фаза «Планування») vs `docs/Product_Overview/Goals.md` | Чекбокс «Goals (довгострокові цілі)» лишався відкритим, хоча коміт `8f06064` уже заповнив цей розділ у `Goals.md` | закрито | чекбокс позначено виконаним з посиланням на коміт |
| ISS-6 | 2026-08-28 | `README.md` §«Документи проєкту» | Перелік не містив `docs/ISSUES.md` і `docs/Product_Overview/Concept.md`, обидва створені цієї сесії — той самий борг, що вже описаний як ДОК-9 | закрито | обидва файли додано в перелік, лічильник «34 файли» оновлено |
| ISS-7 | 2026-08-29 | `docs/DECISIONS.md` D-83 vs `docs/features/structure/spec.md` | D-83 (T2, Крок 5 опитувальника) визначає 3 підваріанти розкладки «за логікою» (3.1/3.2/3.3) як реальні опції продукту, які користувач перемикає вільно — але `structure/spec.md` досі описує лише один варіант «за логікою» без підваріантів; жодного нового US/AC, запису в `data-model.md`, задачі в `tasks.json` чи тесту в `test-plan.md` не додано. Той самий тип дрейфу, який [D-91](DECISIONS.md#d-91) закрив для `agent`/`life-area-card`, лишився невиправленим для `structure` | відкрито | — |
| ISS-8 | 2026-08-30 | `docs/DELIVERY-PLAN.md` §«Критичні блокери» vs `docs/DECISIONS.md` D-12 | DELIVERY-PLAN додає другий тригер розблокування — «або свідоме рішення підняти без Docker» — якого немає ні в рядку реєстру D-12, ні в детальному описі «Відкладене» (там лишається лише один тригер: «поява автономного агента, що працює довго й без нагляду») | відкрито | — |
| ISS-9 | 2026-08-30 | `docs/DELIVERY-PLAN.md` чек-лист `/sdd:api` (agent, life-area-card) vs фактичні `contracts/openapi.yaml` | Чек-лист показував застарілі числа: agent «8 операцій, 9 US, 17 AC» (факт після D-89/D-91 — 12 операцій, 14 US, 27 AC), life-area-card «10 операцій, 14 US, 17 AC» (факт — 11 операцій, 16 US, 20 AC). Після D-89/D-91 позначку «доповнено» отримали лише `tasks.json`/`test-plan.md`, рядки `/sdd:api` — ні | закрито | 2026-08-30: обидва рядки доповнено актуальними цифрами з посиланням на D-89/D-91 — [DELIVERY-PLAN.md §Частина 1 і §Частина 3](DELIVERY-PLAN.md) |
| ISS-10 | 2026-08-30 | `docs/DECISIONS.md` D-88 (RICE) vs фактичний обсяг після D-91 | D-88 (2026-08-29, ухвалено раніше того ж дня) рахує Effort на базі «86 задач / 22 ендпоінти / 13 екранів» — цей обсяг виріс пізніше того ж дня рішенням D-91 (86→111 задач, ендпоінти → 29) | закрито | 2026-08-30: свідомо не перераховуємо і не редагуємо D-88 — сам він визначає тригер перерахунку («після перших реально закритих задач»), а не зміну обсягу оцінки; базові цифри лишаються історичним знімком стану на момент ухвалення |
| ISS-11 | 2026-08-30 | Патерн повторюється: ISS-1, ISS-4, ISS-7, ISS-9 | Коли рішення одного дня зачіпає кілька документів (наприклад D-89→D-91 того ж 2026-08-29), частина похідних місць оновлюється (`tasks.json`/`test-plan.md`), інші (описові рядки чек-листів, бази оцінок типу RICE) — ні; це не ловить той-таки день (`pm-end` сесії вже пройшов), спливає лише наступного дня в `pm-start` | відкрито | — |
| ISS-12 | 2026-08-30 | `docs/DELIVERY-PLAN.md` §«Критичні блокери» та «Наступний крок» vs `docs/DECISIONS.md` D-12 і `.devcontainer/`/`docker-compose.yml` | Документ називав причиною непіднятого бекенд-середовища «Docker відкладено (D-12)» — але D-12 і файли `.devcontainer/`/`docker-compose.yml` стосуються виключно ізоляції самого агента Claude Code (firewall, NET_ADMIN), не бази даних застосунку. Реальна причина — не зареєстрована PostgreSQL ([ADR-0005](adr/0005-backend-datastore.md)), яка не потребує Docker узагалі (безкоштовний хостинг Supabase/Neon/Render). Приклад патерну ISS-11: формулювання внесене вчора, 2026-08-29, комітом `36fad9d`, саме під час виправлення ISS-1 — не «стара» помилка, а нова, що виникла з попереднього виправлення | закрито | 2026-08-30: обидва місця (рядок «Наступний крок» і таблиця «Критичні блокери») переписано на реальну причину — [DELIVERY-PLAN.md](DELIVERY-PLAN.md) |

## Пов'язані документи

- [DECISIONS.md](DECISIONS.md) — рішення, що виникли з розв'язаних нестиковок
- [DELIVERY-PLAN.md](DELIVERY-PLAN.md) — статус проєкту, де нестиковки найчастіше й трапляються
- [CLAUDE.md](../CLAUDE.md) — правило, коли й хто сюди пише
