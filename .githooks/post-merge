#!/bin/sh
#
# post-merge — підказка після `git pull` (Git виконує саме post-merge; хука з
# назвою "post-pull" у Git не існує).
#
# Навіщо: на старті сесії зручно одразу бачити статус проєкту, а не гортати
# документи вручну. Сам хук нічого не читає й не рахує — це робить skill
# session-brief всередині сесії Claude Code. Hook лише друкує підказку, бо
# запустити Claude-сесію напряму з git hook неможливо.
#
# Ніколи не блокує: завжди завершується exit 0.
#
# Довідка: .claude/skills/session-brief/SKILL.md

echo ""
echo "📥 git pull завершено."
echo "→ У сесії Claude Code запусти: /session-brief — побачити поточний статус ПЛАН."
echo ""

exit 0
